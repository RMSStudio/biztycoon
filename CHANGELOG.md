# BizTycoon — Changelog

## v3.30 — Древо 2.0: осведомлённость о engine.UPGRADES + Effects Summary (Фаза B, шаги 2-lite + 7) (2026-06-15)

Tree 2.0 теперь знает про существующую engine-прокачку (без её
замены) и показывает игроку суммарные эффекты от купленных
узлов. Шаг 2 lite — узлы tier 1–2 объявляют `upgradeAlias[]`
с пересекающимися engine-апгрейдами; шаг 7 — UI-блок «Итоговые
эффекты древа» в модале.

**Тип архитектуры: A** — расширение `src/livingmarket.js` v0.4 → v0.5.
Ядро не тронуто. Старая прокачка остаётся отдельной системой —
решение «убирать ли её» откладывается (для шага 2 «полного» нужно
решение Романа).

### Шаг 2 lite — `upgradeAlias[]` на узлах tier 1–2

9 узлов получили ссылку на пересекающийся engine-апгрейд:
- `craft1` (+2 Q) → `tools_q` (+4 Q)
- `craft2` (penaltyShield) → `standards_q`
- `prod1` (+5% speed) → `agile` (+10%)
- `prod2` (+5% speed + instant) → `scrum` (+15%)
- `peop2` (fatigue × 0.9) → `mentorship`
- `mark1` (+5 portfolio) → `portfolio_site`
- `mark2` (+1 caseRepBonus) → `case_studies`
- `deal1` (+5% payout) → `contracts`
- `deal2` (+10% prepay) → `negotiator`

API **`getDuplicatedEngineUpgrades(nodeId)`** → `string[]` из
`G.upgrades`, которые узел "повторяет" (пересекается тематически,
эффекты сложатся). Возвращает только реально купленные апгрейды.

Покупка одного **не блокирует** другое — обе системы работают
параллельно. UI просто предупреждает: «⚠ Дублирует: 🖥️
Проф. инструментарий» под описанием узла, если соответствующий
engine-апгрейд уже куплен.

### Шаг 7 — Effects Summary

API **`getEffectsSummary()`** → агрегат текущих эффектов tree 2.0:
```
{
  numeric: { caseQBonus: { total: 12, contributors: [{id,name,icon,delta}] } },
  mul:     { scoutSalaryMult: { total: 0.81, contributors: [...] } },
  flags:   { perkPenaltyShield: { value: true, contributors: [...] } }
}
```

В модале «Древо 2.0» под сеткой узлов появился блок
«📊 Итоговые эффекты древа»:
- Numeric каналы — итог + список узлов-источников (`+12 · 🛠 Инструментарий · 🎨 Школа арт-директорства`).
- Mul-каналы — `×0.81 (-19%)` + источники с множителями.
- Boolean флаги — `включён · 📐 Дизайн-система`.

Проценты для `perkPayoutMult` / `perkPrepayBonus` / `perkRecoveryBonus` /
`speedUpgrades` форматируются как `+N%`. Если ничего не куплено —
блок скрыт.

API **`getChannelLabels()`** → карта `channelKey → human-readable`
(например `caseQBonus → '🎯 Качество'`) для переиспользования
в других UI.

### Валидация

- `sim/test-livingmarket.js` 185 → **221/221** (тесты 49–57:
  API доступен, 9 узлов tier 1–2 с `upgradeAlias`,
  `getDuplicatedEngineUpgrades` пустой до покупки + список после,
  `getEffectsSummary` пустой до покупок, numeric суммирует с
  contributors, mul перемножает, flags перечисляет источники,
  после respec ветки summary очищается, `getChannelLabels`
  выдаёт русские подписи).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-runmap.js` 136/136,
  `sim/test-live-switch.js` 25/25, `sim/test-strategy.js` ✅,
  `sim/test-outsource.js` 27/27.
- `sim2-lc` agency 12 — 54 сдачи / 2 ухода / 0 ошибок;
  bank 4 — 20 сдач / 2 ухода / 0 ошибок.
- `build` multi 972.5 KB (+8.3 KB) / bank 857.0 KB (+8.3 KB).

### По Фазе B осталось

- **Шаг 2 полный** — собственно маппинг engine.UPGRADES в tree 2.0
  с отключением старой прокачки. Требует решения Романа.
- **Шаг 6 (опц.)** — refcount boolean-флагов для чистого респека.

---

## v3.29 — Древо 2.0: респец ветки (Фаза B, шаг 3) (2026-06-15)

Респец из дизайн-дока §4 («раз в стадию — один бесплатный
respec ветки»). Игрок может сбросить любую ветку и получить
обратно все потраченные ★XP — стиль билда теперь обратимый.
Tier 4 a/b развилки тоже можно пересмотреть.

**Тип архитектуры: A** — расширение `src/livingmarket.js` v0.3 → v0.4.
Ядро не тронуто.

### Механика снапшота + delta

Каждая покупка узла теперь снимает снимок отслеживаемых каналов
G до и после `node.apply(G)` и сохраняет разницу в
`G.living.tree2.purchasedDetails[id]`. Формат:

- **numeric** (`caseQBonus`, `perkPayoutMult`, …) → просто число
  (например `{ caseQBonus: 5 }`)
- **multiplicative** (`scoutSalaryMult`, `perkFatigueMult`) →
  `{ scoutSalaryMult: { mul: 0.9 } }`
- **boolean** (`perkPenaltyShield`, `perkInstantSpeed`,
  `perkEpicShortcut`) → `{ perkPenaltyShield: { setBool: true } }`

На респеке inverse-apply вычитает numeric, делит на mul, флаги
**не снимает** — см. ограничение ниже.

### API

- **`canRespecBranch(branchId)`** → `{ok, nodes, stage}` или
  `{ok:false, reason}` с reason ∈ {`no_active_game`,
  `no_nodes_owned`, `already_used_at_stage`}.
- **`respecBranch(branchId)`** → `{ok, refunded, removed, stage}`.
  Возвращает суммарный XP всех узлов ветки, удаляет их из
  `purchased[]`, инвертирует delta каждого узла на G, пишет в
  `G.living.tree2.respecsUsed[]` + журнал `respec_<branch>_<stage>`,
  эмитит `tree_branch_respec` + `render`.
- **`getRespecsUsed()`** → массив записей `{branch, stage, ts,
  refunded, nodes}`.
- **`getNodeDelta(id)`** → сохранённая дельта эффектов узла
  (для диагностики/UI).

### Лимит

Один бесплатный респец на ветку на стадию. После Гаража → Студии
лимит сбрасывается, можно сбросить ту же ветку снова. Это
поощряет менять стиль билда при больших переходах прогрессии.

### UI

В шапке каждой колонки ветки появилась кнопка «↺ Респец». Активна,
если есть купленные узлы и респек ещё не использован на текущей
стадии. При disabled — tooltip объясняет причину (нет узлов /
уже использовано на этой стадии). После клика — модал
перерисовывается, показывает обновлённые состояния всех карточек.

### Ограничение (known)

Boolean-флаги (`perkPenaltyShield`, `perkInstantSpeed`,
`perkEpicShortcut`) после респека **не снимаются**. Причина:
тот же флаг могут включить мета-перки или runMap-бонусы, и
снимать его на основании респека одного узла было бы небезопасно.
Refcounting (учёт «сколько источников включили флаг») вынесен
в потенциальный шаг 6 Фазы B. Тест 48 явно фиксирует это
поведение, чтобы изменение в будущем было осознанным.

### Валидация

- `sim/test-livingmarket.js` 146 → **185/185** (тесты 38–48:
  API доступен, `no_nodes_owned` до покупок, respec возвращает
  XP и обнуляет numeric, mul-канал делится обратно,
  `already_used_at_stage` лимит, переход стадии разблокирует
  респец, ветки независимы, журнал `getRespecsUsed`, повторная
  покупка после респека, known limitation boolean-флага).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-runmap.js` 136/136,
  `sim/test-live-switch.js` 25/25, `sim/test-strategy.js` ✅,
  `sim/test-outsource.js` 27/27.
- `sim2-lc` agency 12 — 64 сдачи / 3 ухода / 0 ошибок;
  bank 4 — 14 сдач / 2 ухода / 0 ошибок.
- `build` multi 964.2 KB (+9.3 KB) / bank 848.7 KB (+9.3 KB).

### По Фазе B осталось

- **Шаг 2** — маппинг 16 существующих перков engine.UPGRADES в
  tree 2.0 ярус 1–2 (требует решения Романа о судьбе старой
  прокачки: убирать или сосуществовать).
- **Шаг 6 (потенциальный)** — refcounting boolean-флагов для
  чистого респека.

---

## v3.28 — Древо 2.0: эксклюзивные развилки tier 4 + persistence (Фаза B, шаги 4 + 5) (2026-06-15)

Развитие древа 2.0: ярус 4 удвоен альтернативными узлами с
взаимоисключениями (стиль билда из дизайн-дока §4) + проверка
persistence через стандартный save→restore механизм
(шаг 5 закрыт тестом без новой логики).

**Тип архитектуры: A** — расширение `src/livingmarket.js` v0.2 → v0.3.
Ядро не тронуто.

### Tier 4: 5 пар (a/b) альтернативных узлов

Каждой ветке tier 4 добавлен альтернативный узел `<branch>4b` с
уникальным эффектом и взаимоисключением через `excludes:['<branch>4']`.
Купив один — игрок закрывает другой. Это первая настоящая
развилка стиля билда в древе:

- **Ремесло**: 🔬 R&D-лаборатория (+10 Q) ⇄ 🎨 Школа арт-директорства
  (+5 Q · +1 восст. реп/мес · +5 портф) — глубокий vs широкий путь
- **Производство**: ⚙️ Конвейер (+15% speed · −1 epic-фаза) ⇄ 💎
  Бутик-режим (+10 Q · +5% recovery) — скорость vs качество
- **Люди**: 🪪 Кадровый резерв (+1 scout) ⇄ 🧘 Корпоративная культура
  (`perkFatigueMult × 0.8`) — ширина пула vs выгорание
- **Рынок**: 🏅 Премии (+10 портф · +5 реп) ⇄ 📡 Медиа-присутствие
  (+2 scout · +5 портф) — престиж vs массовость
- **Сделки**: 🛡 Демпинг-защита (+15% prepay · shield) ⇄ ⚔️
  Финансовый агрессор (+15% payout) — защита vs давление маржой

Всего узлов в древе: 25 → 30 (на tier 4 — 10 узлов в 5 парах).

### API: getConflictingTreeNodes + excluded_by

- **`getConflictingTreeNodes(id)`** → `string[]` — список id всех
  узлов, конфликтующих с указанным. Поиск двунаправленный
  (direct excludes + inverse), по образцу `meta.js
  getConflictingPerks` v0.5.
- **`canPurchaseNode(id)`** теперь возвращает
  `{ ok: false, reason: 'excluded_by', blocker: '<id>', node }`
  если конфликтующий узел уже куплен. Порядок проверки:
  unknown_node → already_owned → locked → **excluded_by** →
  not_enough_xp.

### UI: «Несовместимо» в карточке узла

- **Узел tier 4 a/b** показывает мини-строку `⇄ или 🎨 Школа
  арт-директорства` под описанием — игрок видит выбор ДО клика.
- **После покупки одного** другой получает статус «⛔ Несовместимо»
  (вместо «Купить · ★N»), карточка приглушена `opacity:.6`,
  рамка красная. Описание остаётся видимым, чтобы было ясно,
  ЧТО игрок отдал, выбрав альтернативу.
- При hover на статус — title с именем блокатора.

### Шаг 5 (persistence) — закрыт тестом

`saves.js` сохраняет `G` целиком в `localStorage`. При load `_restore`
копирует все поля обратно — включая мутации из tree 2.0 effects
(`G.caseQBonus`, `G.perkPayoutMult`, …). Эффекты НЕ нужно
переприменять после reload — они уже в G и сохраняются вместе
с ним. Контракт проверен тестом 36: после `JSON.parse(JSON.stringify(G))`
+ полного `initState()` + restore полей — `G.caseQBonus`, `G.xp`,
`purchased[]` сохранены, повторная покупка `craft1` отдаёт
`reason: 'already_owned'`.

Если кто-то изменит саве-логику и эффекты начнут теряться —
тест упадёт. Открытая работа (отдельный шаг) — переприменять
эффекты ТОЛЬКО для очень старых сейвов из времени до v3.27 (там
purchased[] есть, но G-мутаций нет). На текущий момент таких сейвов
у пользователей нет.

### Валидация

- `sim/test-livingmarket.js` 103 → **146/146** (тесты 31–37:
  10 узлов tier 4 в 5 парах, excludes двунаправленны,
  `getConflictingTreeNodes` direct + inverse + unknown,
  покупка a блокирует b с `excluded_by` + blocker,
  inverse-симметрия (b блокирует a), эффекты 4b отличаются от 4a,
  persistence save+restore сохраняет caseQBonus/xp/purchased,
  tier 5 без excludes).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-runmap.js` 136/136,
  `sim/test-live-switch.js` 25/25, `sim/test-strategy.js` ✅,
  `sim/test-outsource.js` 27/27.
- `sim2-lc` agency 12 — 51 сдача / 2 ухода / 0 ошибок;
  bank 4 — 15 сдач / 3 ухода / 0 ошибок.
- `build` multi 954.9 KB (+4.7 KB) / bank 839.4 KB (+4.7 KB).

### Открытое по Фазе B

- **Шаг 2** — маппинг 16 существующих перков engine.UPGRADES в
  tree 2.0 ярус 1–2. Требует решения Романа: «убирать ли старую
  прокачку или это второй накопительный канал».
- **Шаг 3** — респек: раз в стадию бесплатный сброс ветки. Требует
  обратимого Apply DSL для каждого узла (значимая переработка).

---

## v3.27 — Живой рынок: древо 2.0 + ★XP (Фаза B, шаг 1) (2026-06-15)

Первый шаг Фазы B из `design_living_market.md`. Введена ★XP-валюта
(основной канал прогрессии прокачки) и древо 2.0 — 5 веток ×
5 ярусов = 25 узлов. Старая прокачка (engine.UPGRADES) пока остаётся
параллельно — маппинг 16 существующих перков в ярусы 1–2 отложен
на следующую итерацию.

**Тип архитектуры: A** — расширение `src/livingmarket.js` v0.1 →
v0.2. Ядро не тронуто.

### ★XP-валюта

`G.xp` — доступный баланс, `G.living.xpEarned` — всего заработано.
Источники начисления (через хелпер `_awardXp(amount, source)`):

- **Завершение проекта**: `10 × tier` ★XP (tier из
  `completedProjects[i].tier`, fallback 1). Diff-трекер
  `G.living._lastDeliveryCount` ловит новые сдачи в обёртке
  `advanceMonth` — без правки `finishDelivery` в projects.js.
- **Майлстоун**: 15 (микро) / 40 (средние) / 120 (крупные) ★XP.
- **Переход стадии**: `200 + (idx × 100)` ★XP. Студия даёт 300,
  Агентство 400. Гараж не даёт (стартовая).

Каждое начисление эмитит `xp_awarded` через EventBus и пишется в
`G.living._xpLog` (fifo последних 10 для UI-всплывашек, в сейв не
сохраняется намеренно).

### Древо 2.0 (25 узлов)

5 веток с цветовой раскладкой:
- 🎯 **Ремесло** (cyan) — качество, ревью, R&D
- ⚡ **Производство** (yellow) — скорость, мощность, фазы
- 🤝 **Люди** (violet) — найм, лояльность, усталость
- 📣 **Рынок** (amber) — репутация, лиды, награды
- 💼 **Сделки** (green) — деньги, переговоры

Tier-гейты по стадии компании:
- **tier 1** (50–60 ★XP) — открыт со старта
- **tier 2** (150–180) — открыт со старта
- **tier 3** (320–350) — со стадии Студия (idx 1)
- **tier 4** (700–720) — со стадии Агентство (idx 2)
- **tier 5** (1200) — со стадии Сеть (idx 3), но физически
  заблокирован: требует модуля конкурентов (Фаза C). Эффекты
  у tier 5 пока no-op, узлы видны в UI как «силуэты».

### Эффекты узлов

Каждый узел — `{ id, branch, tier, icon, name, desc, cost, apply: g => {...} }`.
Эффекты идут через те же G-каналы, что и runMap-бонусы /
мета-перки, — суммируются без двойного учёта:

`caseQBonus`, `speedUpgrades`, `perkFatigueMult`, `perkRecoveryBonus`,
`perkPayoutMult`, `perkPrepayBonus`, `perkPenaltyShield`,
`caseScoutBonus`, `caseRepBonus`, `portfolio`, `reputation`,
`scoutSalaryMult` (новый канал HR-бренда — кандидаты дешевле),
`perkInstantSpeed` (новый флаг для instant-проектов, активен с
шаблонов prod2), `perkEpicShortcut` (epic-цепочки −1 фаза, активен
с конвейера prod4).

### API

- `getXp()` / `getXpEarned()` — баланс и общая статистика.
- `getTreeBranches()` / `getTreeNodes()` / `getTierStageGates()` —
  декларация для UI.
- `isNodeUnlocked(id)` — открыт ли tier по стадии (без проверки XP).
- `canPurchaseNode(id)` → `{ok, reason: 'unknown_node' | 'already_owned' |
  'locked' | 'not_enough_xp', node}`.
- `purchaseTreeNode(id)` — списывает XP, добавляет id в
  `G.living.tree2.purchased[]`, вызывает `node.apply(G)`, пишет в
  journal, эмитит `tree_node_purchased` + `render`.
- `showTreeModal()` — открыть UI древа.

### UI

- **Модал «Древо 2.0»**: 5×5 сетка карточек, заголовки веток с
  цветной полоской снизу, у каждого яруса — пунктирная подпись
  («Ярус 3 · 🔒 откроется со стадии «Агентство»» или просто
  «Ярус 4»). Карточка узла показывает иконку, имя, описание,
  кнопку покупки (или статус ✓ Куплен / 🔒 Заблокирован / ★N
  (не хватает)). XP-баланс в шапке справа.
- **Кнопка «Древо 2.0 →»** в модале журнала (рядом с XP-блоком),
  не выносится в отдельную пилюлю — оба окна про прогрессию.
- **★XP в пилюле стадии** (`.game-header .game-logo`) — справа,
  только если `xp > 0`.
- **Покупка узла** обновляет журнал и перерисовывает модал
  древа на месте.

### Стейт

- `G.xp` — баланс (числом).
- `G.living.xp` / `G.living.xpEarned` / `G.living._xpLog` —
  агрегаты и FIFO-лог.
- `G.living.tree2 = { purchased: [...] }` — id купленных узлов;
  переживает сейв; эффекты переприменяются заново в обёртке
  `startGame` (TODO: на самом деле сейчас НЕ переприменяются при
  загрузке, потому что `purchaseTreeNode` мутирует G напрямую,
  а не сохраняет «надо применить». Это OK для первой итерации —
  в следующей сделаем restore-проход).
- `G.living._lastDeliveryCount` — diff-трекер сдач.

### Валидация

- `sim/test-livingmarket.js` 55 → **103/103** (тесты 18–30: API
  древа доступно, 5×5 узлов, tier 1–2 открыты со старта, tier 3
  открывается на Студии, tier 5 заблокирован даже на Сети,
  переход стадии даёт 300 ★XP, майлстоуны 15/40/120, сдачи
  10×tier, покупка списывает XP и применяет эффект, повтор →
  already_owned, без XP → not_enough_xp, без стадии → locked,
  эффекты разных веток корректно мутируют G, журнал получает
  запись tree_<id>).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-runmap.js` 136/136,
  `sim/test-live-switch.js` 25/25, `sim/test-strategy.js` ✅,
  `sim/test-outsource.js` 27/27 — все зелёные.
- `sim2-lc` agency 12 — 56 сдач / 2 ухода / 0 ошибок;
  bank 4 — 17 сдач / 2 ухода / 0 ошибок.
- `build` multi 950.2 KB (+25.7 KB) / bank 834.7 KB (+25.7 KB).

### Что НЕ сделано (планируется отдельно)

- **Маппинг 16 существующих перков в tree 2.0 tier 1–2** — следующая
  итерация (нужно мерджить с engine.UPGRADES без двойных эффектов).
- **Респек** (раз в стадию — бесплатный сброс ветки) — отдельная
  итерация: возврат XP + откат node.apply(g).
- **Эксклюзивные развилки tier 4** (выбор из двух, второй
  закрывается) — нужна `excludes:[]` декларация и переработка
  purchase API.
- **Восстановление эффектов из purchased[] на load** — для сейвов
  через сессии (см. TODO выше).

---

## v3.26 — Живой рынок: каркас фазы A (стадии + майлстоуны) (2026-06-15)

Первый шаг внедрения «Живого рынка» из `design_living_market.md`.
По уточнению Романа — **не DLC, а большой подключаемый модуль ядра**
актуальный для всех версий игры. Архитектурно ведёт себя как
`outsource` (v3.8.1): один флаг сверху файла, обёртки глобалов,
никаких правок ядра. Прогресс по п.14 бэклога.

**Тип архитектуры: A** — `src/livingmarket.js` v0.1, флаг
`LIVING_MARKET_ENABLED = true` по умолчанию. Engine/staff/projects/ui
не изменены, кроме разкомментирования оригинальной проверки
`winCondition` в `src/engine.js` (она была в WIP-комменте от прежней
сессии). Модуль управляет win-условием через мутацию
`SCENARIO.settings.winCondition = Infinity` в обёртке `startGame`.

### Каркас стадий компании (6 стадий, 3 живых)

Лестница из дизайн-дока §2 — Гараж → Студия → Агентство → Сеть →
Холдинг → Империя. В этом шаге реализованы первые три (Гараж/
Студия/Агентство) — гейты считаются по `completedProjects.length`,
`staff.length`, `reputation`, накопленной выручке (сумма по
`completedProjects[].revenue`). Стадии 4–6 объявлены, но помечены
`requiresMarket: true` — их гейты требуют рейтинга / наград /
поглощений из модуля конкурентов (Фаза C дизайн-дока).

Композитные гейты с детализацией по каждому условию:
- **Гараж** — старт.
- **Студия** — 3 сдачи · штат 2 · накопленная выручка 2M ₽.
- **Агентство** — 10 сдач · штат 5 · реп 75 · 15M ₽ суммарно.

При переходе — модал-церемония с иконкой/цветом стадии и списком
открывающихся механик. Запись пишется в `G.living.journal` с tier
`large`.

### Снятие классического win-условия

`startGame`-обёртка ставит `SCENARIO.settings.winCondition = Infinity`
(если модуль активен). Engine продолжает читать это значение в
`advanceMonth` — win-экран никогда не триггерит. Оригинальное число
сохраняется в `G.living.originalWinCondition` и используется как
порог для майлстоуна «Старый порог». Когда модуль выключен —
работает как раньше (классический win при достижении 7.5M/9M).

### Майлстоуны (декларативный фреймворк)

Встроенный пул v0.1 — 11 майлстоунов трёх эшелонов (микро/средние/
крупные). Каждый: `{ id, name, icon, desc, tier, cond(g) }`,
проверка в обёртке `advanceMonth`, сработавшие пишутся в
`G.living.journal` и эмитят сигнал `milestone_reached` через
EventBus + тост через `notify`.

Микро: первая сдача · репутация 50 · первый найм · первый миллион ·
пять сдач. Средние: команда из 5 · выручка 5M · портфолио 25.
Крупные: старый порог 7.5M/9M · стадии Студия/Агентство.

В следующей итерации пул переедет в `scenarios/<id>.data.js →
milestones[]` (Тип C) — пока встроен в модуль.

### UI

- **Пилюля стадии** в `.game-header .game-logo`: иконка стадии,
  название, прогресс ближайшего гейта («2/3 сдач · 1/2 штат ·
  1.4M/2M ₽»). По клику — журнал.
- **Модал церемонии** при достижении стадии: 64px эмодзи, название,
  что открылось, кнопка «Продолжить» (только модул-уровневая
  обёртка, без правок ui.js).
- **Модал журнала**: блок «Следующая стадия» (прогресс-бары
  условий) + список достижений (от новых к старым), tier-полоска
  слева, метка месяца. Запертые стадии 4–6 показаны как
  пунктирные строки «🔒 Откроется с модулем «Живой рынок» (фаза C)».

### Стейт

- `G.living = { stage, journal, milestonesFired, originalWinCondition }`
  — переживает сейв партии (saves.js сериализует G целиком,
  `_restore` копирует поля).
- `G.living._peakMoney` — рантайм-трекер пика кассы за партию
  (для майлстоунов типа «крест порога»; в сейв не пишется
  намеренно, восстановится с `_runMaxMoney` из meta.js или с
  `G.money` после первого тика).

### Чего НЕ делает (планируется отдельными шагами)

- Годовые итоги (M12/M24/…) — следующая итерация фазы A.
- Живые конкуренты, рейтинг, награды — Фаза C.
- Древо 2.0 (★XP, 5 веток) — Фаза B.
- Активы (офисы, саббренды) и M&A — Фаза E.
- Перенос пула майлстоунов в данные сценария — следующая итерация.

### Engine-правка (одна строка)

В `src/engine.js` раскомментирована оригинальная проверка
`if (G.money>=SCENARIO.settings.winCondition){...}` (была в
WIP-комменте от прежней сессии). При выключенном
`LIVING_MARKET_ENABLED` это вернёт классический win-экран без
дополнительных правок.

### Валидация

- `sim/test-livingmarket.js` (новый) — **55/55** (тесты 1–17:
  API, kill-switch, подмена winCondition, инициализация G.living,
  отсутствие engine-win при Infinity, переход Гараж→Студия и
  Студия→Агентство по гейту, частичные гейты не двигают, стадия
  не откатывается, стадии 4–6 заблокированы, майлстоуны one-shot
  c журналом и не дублируются, original_win по пику кассы, обёртки
  идемпотентны, композитный гейт критичен к каждому условию).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-runmap.js` 136/136,
  `sim/test-live-switch.js` 25/25, `sim/test-strategy.js` ✅,
  `sim/test-outsource.js` 27/27 — все зелёные.
- `sim2-lc` agency 12 — 59 сдач / 4 ухода / 0 ошибок;
  bank 4 — 19 сдач / 1 уход / 0 ошибок. (Сим не подгружает
  livingmarket.js → engine использует оригинальный winCondition,
  бот всё ещё фиксирует «победу» через свой G.money ≥ winCondition.)
- `build` multi 924.5 KB (+31.8 KB) / bank 809.0 KB (+31.8 KB).

---

## v3.25 — Балансовая дифференциация banking-runMap (2026-06-15)

Бонусы Run Map банковского сценария перевзвешены под банковский
профиль: целевой капитал 9M против 7.5M у агентства, overhead
85K/мес против 35K (×2.4 в абсолюте, в реальном сценарии давит
сильнее ещё за счёт большего временного окна по T-гейтам),
бóльшие сделки на T5–T7 (18M–150M против 6M–35M).

**Тип архитектуры: C** — только данные `scenarios/bank.data.js →
runMap.bonuses[]`, через DSL `effects[]`. Ядро не тронуто
(engine/staff/projects/ui/`src/runmap.js` — без правок).
Прогресс по п.13 (Roguelite-механики, дифференциация).

### Логика перевзвешивания

- **Overhead давит сильнее** → бонусы снижения overhead весомее
  (`automation` −10% → −12%, эксклюзив `initial_capital` остался
  −15% как ранее).
- **Сделки крупнее** → флэт-money бонусы выросли (`deposit_base`
  250K → 400K, эксклюзивы `core_banking` 400K → 550K, `spo_capital`
  800K → 1.2M, `mna_acquisition` 500K → 700K, `gov_contract`
  250K → 350K).
- **Маржа на больших сделках значимей** → payout-бусты подросли
  (`margin` +5% → +6%, `fx_desk` +7% → +8%, `vip_office` +10% →
  +12%).
- **Предоплата корпоратов критичней** → `legal_team` +15% → +20%.
- **Portfolio-гейты жёстче** в позднем банке → `case_studies` +10
  → +12, `mna_acquisition` +15 → +20.
- **Репутация до 80+ для VIP/private** → `trust` +5 → +6,
  `systemic_status` +5 → +6.
- **Скоринг — банковская специализация** → `scoring` Q+5 → +6,
  `syndicate_lending` Q+5 → +6.
- **Выгорание команды банка выше** → `wellbeing` усталость −15 →
  −18, восстановление +5% → +6%.

### Без изменений (cap / непрофильное)

- `branch_network` — лиды +1 (cap скаута не масштабируется)
- `pr_dept` — реп/мес +1 (тот же канал что у агентства)
- `rpa` — скорость +5% (на банковском темпе не отдельная вещь)
- `regulator` / `systemic_status.shield` — penalty shield bool
- `wealth_advisors` / `mobile_app` / `initial_capital` — уже
  сбалансированы под этап

### Структура без правок

ID, этапы, ключи `effects[]` и DSL — не тронуты: 12 универсальных
+ 11 этап-эксклюзивов = 23 бонуса (та же что v3.19). Это значит
все существующие тесты на структуру (16–18) и сейвы продолжают
работать без миграций.

### Валидация

- `sim/test-runmap.js` 108 → **136/136** (тесты 19–20: проверка
  каждого изменённого значения по `byId(...).effects[...]` + тест-
  страховка, что agency значения остались прежними).
- `sim/test-meta.js` 191/191, `sim/test-runes.js` 27/27,
  `sim/test-storyarcs.js` 67/67, `sim/test-live-switch.js` 25/25,
  `sim/test-strategy.js` ✅, `sim/test-outsource.js` 27/27.
- `sim2-lc` agency 12 — 52 сдачи / 5 уходов / 0 ошибок;
  bank 4 — 22 сдачи / 3 ухода / 0 ошибок.
- `build` multi 892.7 KB / bank 777.2 KB.

---

## v3.24 — Дерево выбора мета-перков + третья волна комбо-ачивок (2026-06-15)

Третий шаг расширения мета-прогресса (п.13 — Roguelite-механики).
Мета-перки получили **взаимоисключения** — теперь это не просто
накопительный список покупок, а первое настоящее «дерево выбора»,
заставляющее игрока определяться с траекторией между ранами.
Параллельно добавлены три новых комбо-ачивки, поощряющие
самоограничение и широту охвата сценариев.

**Тип архитектуры: B** — `src/meta.js` v0.4 → v0.5. Ядро не
тронуто (engine/staff/projects/ui — без правок). Активируется
только при включённом DLC «Rogue-lite», как и предыдущие шаги.

### Дерево выбора (+2 мета-перка с excludes)

- 🏦 **`early_advance`** (350 ✦) — *Стартовый аванс*: +30% к
  шансу предоплаты с самого старта (`G.perkPrepayBonus += 0.30`).
  Альтернатива деньгам: вместо +100К кэшем — более частые авансы
  весь ран. `excludes: ['seed_money']`.
- 🧙 **`solo_genius`** (300 ✦) — *Гений-основатель*: +5 Q
  стартового `G.qualityBonus` — растёт без команды (для соло-стиля).
  `wise_consult` усиливает наёмных, `solo_genius` — самого
  основателя. `excludes: ['wise_consult']`.

Покупка взаимоисключающего перка отклоняется с
`{ ok: false, reason: 'excluded_by', blocker: '<id>' }`. Проверка
двунаправленная: если этот перк указан в чьих-то `excludes` —
тоже блок (например, `seed_money` без своих excludes блокируется
тем, что `early_advance.excludes` его перечисляет).

### Третья волна комбо-ачивок (+3)

- 🌐 **`scenario_master`** (300 ✦) — победа в обоих сценариях
  (agency и bank). Трекер `meta.wonScenarios[]` накапливается на
  каждой победе. `progress: {cur, max:2}`.
- 💸 **`debt_free`** (150 ✦) — победа без единого кредита за
  партию. Трекер `G._loanTakenEver` поднимается в обёртке
  `advanceMonth` при появлении `G.loan`, в history-записи —
  `loanTaken: !!g._loanTakenEver`.
- 👤 **`minimalist`** (200 ✦) — победа с командой не более 1
  человека. Проверяется по уже существующему `run.staffCount` из
  v0.4. Распространение «соло-режима» на более мягкий уровень:
  если `solo_win` (300 ✦) — это полный 0, то `minimalist` (200 ✦) —
  до одного помощника.

### API

- **`getConflictingPerks(id)`** → `string[]` — список id всех
  перков, конфликтующих с указанным (direct + inverse). Используется
  UI-рендером и валидацией.
- **`purchaseMetaPerk(id)`** теперь возвращает
  `{ ok: false, reason: 'excluded_by', blocker: '<id>' }` при
  попытке купить взаимоисключающий вариант.

### UI

В модале «🎁 Мета-перки — навсегда» появилось три новых статуса
карточки:
1. **✓ Куплен** — куплен лично (зелёная пилюля, как раньше).
2. **⛔ Заблокирован** — куплен взаимоисключающий вариант
   (красная пилюля с иконкой блокирующего перка в названии,
   карточка приглушена `opacity:.65`).
3. **Купить · N ✦** — обычная покупка (как раньше, серая при
   нехватке ✦).

Под описанием каждого перка с конфликтами — мини-строка
`⛔ Несовместимо: 🏦 Стартовый аванс` — даже до покупки видно,
что один из вариантов закроет другой.

### Стейт

- `meta.wonScenarios: string[]` — новый поле, переживает сейв
  партии (хранится в отдельном `bt_roguelite_meta_v1`).
- `run.scenarioId: string|null` — записывается в каждый history.
- `run.loanTaken: boolean` — записывается в каждый history.
- `G._loanTakenEver: boolean` — runtime-флаг, выставляется
  обёрткой `advanceMonth`, не сериализуется в meta-сейв
  (трекинг идёт в рамках одного рана).

### Валидация

- `sim/test-meta.js` 147 → **191/191** (тесты 41–50: API
  getConflictingPerks с двунаправленностью + 4 кейса, excluded_by
  для обоих направлений + reset между, эффекты early_advance/
  solo_genius на G.perkPrepayBonus/qualityBonus, scenario_master
  positive + progress, debt_free positive/negative, minimalist
  positive/negative, обёртка `advanceMonth` выставляет
  `_loanTakenEver`, scenarioId в history).
- `sim/test-runes.js` 27/27, `sim/test-storyarcs.js` 67/67,
  `sim/test-runmap.js` 108/108, `sim/test-live-switch.js` 25/25,
  `sim/test-strategy.js` ✅, `sim/test-outsource.js` 27/27.
- `sim2-lc` agency 12 — 44 сдачи / 4 ухода / 0 ошибок;
  bank 4 — 27 сдач / 3 ухода / 0 ошибок.
- `build` multi 889.0 KB (+7.0 KB) / bank 773.5 KB.

---

## v3.23 — Политики автопилота Monte-Carlo в Strategy DLC (2026-06-15)

Strategy DLC получил **выбор политики автопилота** для Monte-Carlo
прогнозов. Раньше двойник в MC всегда вёл себя по одному
зашитому алгоритму («консервативный без найма») — теперь это
одна из трёх предустановок, выбираемая перед запуском прогона.

**Тип архитектуры: B** — расширение `dlc/strategy/strategy.js`.
Ядро не тронуто. Прогресс по п.15 (B2B strategy-DLC).

### Три политики

- 🪨 **`as_is`** — пассивный наблюдатель. Только решает
  work-события безопасным вариантом и расставляет команду.
  Не скаутит, не подписывает, не нанимает. Отвечает на
  «что будет, если вообще ничего не менять».
- 🧭 **`conservative`** (дефолт, исторический) — ведёт контракты,
  добирает T1–T3 в свободные слоты, расставляет команду —
  но НЕ нанимает. Отвечает на «что будет, если продолжать
  текущую линию».
- 🚀 **`aggressive`** — давит на рост. Скаутит и подписывает
  агрессивно (T1–T7, приоритет epic), нанимает middle/senior
  через `hireStaff`, если касса > 800К и активных проектов ≥ 2.
  Отвечает на «что будет при масштабировании без оглядки».

### Архитектура

Логика бота вынесена в фабрику `_makeMcBotMonth(policyId)` и
три общих хелпера: `_mcResolveWorkEvents`, `_mcReassignTeam`,
`_mcScoutAndSign(slotCap, tierCap, preferEpic)`, `_mcHireOne()`.
Это позволяет смешивать поведение без копипасты — каждая
политика собирается из 2–3 строк.

API `runMonteCarlo` теперь принимает:
- `runMonteCarlo(60)` — число (бэк-компат, политика
  `conservative` по умолчанию)
- `runMonteCarlo({ runs: 60, policy: 'aggressive' })` —
  объект с явной политикой

Неизвестная политика автоматически фоллбэчит на `conservative`
без ошибки (`MC_POLICIES[opts.policy] ? ... : 'conservative'`).
Имя политики записывается в `G._strategyMode.lastMC.policy`
и отображается в заголовке модала результатов
(`🎲 Monte-Carlo · N прогонов · K мес. вперёд · 🚀 Рост на максимум`)
и в подписи под гистограммой (`_mcPolicyTail`).

### UI

Кнопка «🎲 Monte-Carlo» в страт-сессии теперь открывает
`openMonteCarlo()` — модал с тремя карточками-политиками,
у каждой подсказка `_policyHint` («что покажет», когда полезно).
Клик по карточке запускает `runMonteCarlo({ policy })`.

Прямой запуск `runMonteCarlo(...)` остаётся публичным API
(для внешних вызовов и тестов).

### Валидация

- `sim/test-strategy.js` — добавлены 12 проверок на политики:
  `getMcPolicies` отдаёт ровно 3 предустановки, `openMonteCarlo`
  экспозирован, каждая политика правильно записывается в
  `lastMC.policy`, реальное состояние не тронуто, бэк-компат
  `runMonteCarlo(4)` → `conservative`, неизвестная политика
  тоже → `conservative`.
- Все остальные тесты без изменений: `test-meta` 147/147,
  `test-runes` 27/27, `test-runmap` 108/108, `test-storyarcs`
  67/67, `test-live-switch` 25/25, `test-outsource` 27/27.
- `sim2-lc agency 12` — 51 сдача / 2 ухода / 0 ошибок.
- `sim2-lc bank 4` — 25 сдач / 4 ухода / 0 ошибок.
- `build multi` — 882.0 KB (без существенного роста; политики
  компактны).
- `build --scenario=bank` — 766.5 KB.

## v3.22 — Вторая волна мета-перков + комбо-ачивки + progress hint (2026-06-15)

Продолжение v3.21: расширение каталога мета-перков (3 → 6),
вторая порция комбо-ачивок (solo_win / no_milestones) и
визуальный прогресс-хинт на ачивках с числовым порогом
(`master_5_wins`, `survivor`, `endgame_reached`, `millionaire`,
`rune_collector`, `win_streak_3`, `difficulty_master`).

**Тип архитектуры: B** — расширение `src/meta.js` v0.3 → v0.4.
Ядро, engine, runes, runmap, storyarcs не тронуты. Один новый
вызов `_applyMetaPerksToG` суммирует эффекты в G через те же
каналы, что и Run Map бонусы.

### +3 мета-перка (вторая волна)

- 🛡 **`penalty_grace`** — **350 ✦**. `G.perkPenaltyShield = true`
  с самого старта рана. Зеркалит milestone-бонус
  `penalty_shield`, но без ожидания M14: штраф просрочки бьёт
  по репутации −50% с первого месяца. Полезно для тяжёлых
  партий на hard/nightmare.
- 🎯 **`signature_lead`** — **250 ✦**. `+1` к
  `G.caseScoutBonus` в начале рана. Один лишний лид при каждом
  скаутинге начиная с месяца 0 — компенсация за низкий старт.
- 🧠 **`wise_consult`** — **200 ✦**. `+3` к `G.caseQBonus` с
  старта. «Стартовая экспертиза» — будто у команды уже есть
  кейс-портфолио, который даёт качество.

Эффекты суммируются с другими источниками (Run Map бонусы,
руны, кейсы) — никакой особой логики. Применяются в
`_applyMetaPerksToG` после оригинального `startGame`, до
фиксации `_runMaxMoney`.

### +2 комбо-ачивки

- 🧘 **`solo_win`** — **250 ✦**. Победа без найма
  единственного сотрудника (`G.staff` пуст). Жёсткий вызов:
  нужно вытащить ран на основателе + аутсорсе. Триггерится
  при `run.staffCount === 0 && run.won`.
- 🚫 **`no_milestones`** — **200 ✦**. Победа без выбора ни
  одного бонуса на milestone-границах Run Map
  (`G.runMap.choicesTaken` пуст). Альтернативный вызов:
  отказаться от стратегической прокачки, играть на «базе».

Снапшот рана теперь содержит `staffCount` и `choicesTaken`
(число выбранных бонусов) — пригодится и для будущих ачивок.

### Прогресс-хинт на ачивках

Опциональное поле `progress: ctx => ({ cur, max })` у элементов
`ACHIEVEMENTS`. Если объявлено и ачивка не получена —
карточка рендерит мини-бар `cur/max` под описанием. Для
`millionaire` числа форматируются в M/K (компактно).

Применено к 7 ачивкам с числовым порогом:
- `endgame_reached` → max(stageReached) / 4
- `master_5_wins` → wins / 5
- `survivor` → последние 3 без банкротств / 3
- `millionaire` → max(peakMoney) / 5 000 000 ₽
- `rune_collector` → играных базовых рун / 4
- `win_streak_3` → побед в последних 3 / 3
- `difficulty_master` → выигранных сложностей / 4

Бар рендерится только когда ачивка не получена. После
получения карточка переключается в «золотой» режим как раньше.

### Валидация

- `sim/test-meta.js` 119 → **147/147** (тесты 33–40: penalty_grace
  выставляет `G.perkPenaltyShield`, signature_lead → caseScoutBonus,
  wise_consult → caseQBonus; solo_win срабатывает только при
  staff=0, no_milestones — только при choicesTaken=0; progress
  API на master_5_wins/rune_collector/difficulty_master отдаёт
  правильные cur/max).
- Тест 3 обновлён: передаёт `staff:[{}]` и
  `runMap.choicesTaken:['x']` чтобы заблокировать новые комбо
  и сохранить ожидание 540 ✦.
- Тест 23 обновлён: пул мета-перков 3 → 6.
- Все остальные тесты без изменений: `test-runes` 27/27,
  `test-runmap` 108/108, `test-storyarcs` 67/67,
  `test-live-switch` 25/25, `test-strategy` ✅, `test-outsource`
  27/27.
- `sim2-lc agency 12` — 58 сдач / 4 ухода / 0 ошибок.
- `sim2-lc bank 4` — 18 сдач / 2 ухода / 0 ошибок.
- `build multi` — 882.0 KB (+5.8 KB к v3.21).
- `build --scenario=bank` — 766.5 KB.

## v3.21 — Мета-перки и мастер сложностей (2026-06-15)

Шаг развития мета-прогресса: накопительные мета-перки (одноразовые
покупки за ✦, действуют во ВСЕХ будущих ранах) + комбо-ачивка
«Мастер сложностей» (победа на всех четырёх пресетах сложности).
Раньше шарды можно было только пассивно копить «до руны/бонуса» —
теперь есть третья категория стартовых инвестиций, которая
накапливается между ранами без сброса.

**Тип архитектуры: B** (расширение `src/meta.js`, ядро не тронуто).
В `src/runes.js` — одна локальная правка: лимит перебросов
2 → `_maxRerolls()` (читает `RogueMeta.getBonusRerolls()` с
back-compat фолбэком на 2).

### Мета-перки (новый раздел в модале)

Три накопительных перка, покупаются за ✦ один раз и навсегда:

- 🎲 **`extra_reroll`** — **250 ✦**. +1 к лимиту перебросов в
  модале выбора руны (2 → 3, в подписи появляется значок «✨»).
- 💰 **`seed_money`** — **300 ✦**. +100 000 ₽ к стартовому
  капиталу в начале любого рана (применяется в обёртке `startGame`
  после оригинального вызова, до фиксации `_runMaxMoney`).
- ⭐ **`brand_starter`** — **200 ✦**. +5 к стартовой репутации
  (поверх руны и пресета сложности).

Эффекты применяются через те же G-каналы (`G.money`,
`G.reputation`) — engine не знает о мета-перках. Сейв при
импорте старого формата без `meta.metaPerks` инициализирует
пустым массивом (back-compat обеспечен `_defaults()`).

### Ачивка «Мастер сложностей»

🎖 **`difficulty_master`** — **300 ✦**. Победа на всех четырёх
пресетах сложности (`easy` / `normal` / `hard` / `nightmare`).
Трекинг через новое поле `meta.wonDifficulties[]` (Set-like, без
дублей при повторных победах на одной сложности). При `end_game`
победа на новой сложности добавляется в массив; ачивка проверяется
наличием всех 4 в списке. Каждая запись `meta.history` теперь
содержит поле `difficulty` (читается из `localStorage
['bt_difficulty_v1']` через `_currentDifficulty()`; дефолт `normal`
если игрок ещё не выбирал).

### UI

В модале «⚡ Мета-прогресс» новый блок «🎁 Мета-перки — навсегда»
(между бонусами Run Map и историей ранов). Карточка каждого перка:
иконка / название / описание / кнопка «Купить · N ✦» или
лейбл «✓ Куплен». Кнопка disabled при нехватке ✦. После покупки —
лог-уведомление «✨ Мета-перк «...» куплен за N ✦» и автоматическая
перерисовка модала + кнопки на mode-screen.

### Архитектурное замечание

Покупка перка необратима (расход ✦ нельзя откатить). Это сознательно:
roguelite-петля «копи между ранами, инвестируй стратегически» работает
только если выбор имеет вес. `RogueMeta.reset()` — единственный способ
вернуть всё (сбрасывает и перки, и shards). Мета-перки не пересекаются
с RUNE_UNLOCKS / BONUS_UNLOCKS — это третий, параллельный канал
прогрессии (стартовые модификаторы поверх рунно-бонусной асимметрии).

### Валидация

- `sim/test-meta.js` 85 → **119/119** (тесты 23–32: API мета-перков,
  отказ покупки при недостатке ✦ / при повторной, успех с списанием
  shards, применение seed_money/brand_starter в startGame, трекинг
  wonDifficulties, ачивка `difficulty_master` на 4-й сложности,
  уникальность записи при повторах, поле `difficulty` в history).
- Все остальные тесты без изменений: `test-runes` 27/27 (новый
  `_maxRerolls()` обратносовместим), `test-runmap` 108/108,
  `test-storyarcs` 67/67, `test-live-switch` 25/25, `test-strategy`
  ✅, `test-outsource` 27/27.
- `sim2-lc agency 12` — 48 сдач / 7 уходов / 0 ошибок.
- `sim2-lc bank 4` — 20 сдач / 2 ухода / 0 ошибок.
- `build multi` — 876.2 KB (+8.9 KB к v3.20).
- `build --scenario=bank` — 760.8 KB.

## v3.20 — Расширение контента Story Arcs (2026-06-15)

Пул сюжетных арок удвоился: было по 2 на сценарий — теперь по 4.
Новые арки демонстрируют то, что движок `src/storyarcs.js` уже
умел с v3.11, но контент-то был тонким: кросс-арочные `requires`
(одна арка зависит от завершённости другой), ветвящиеся графы
(`choices` ведут на разные `next` со своими стадиями), три варианта
в одной развилке, разные «развязки» в зависимости от ветки.

**Тип архитектуры: C** — только данные в `scenarios/<id>.data.js`,
движок и DSL не трогаем.

### Агентство — +2 арки

**📞 `pivot_offer`** — «Алексей возвращается с офером на пивот»
*requires `old_friend` (продолжение арки v3.11)*, minMonth 10,
maxMonth 28, cooldown 6, 3 стадии. Бывший знакомый предлагает
переключиться на IT-аутсорс для «Берёзки» — стабильный фикс,
но это уже не агентская работа. Развилка через `next`:

- **intro** → выбор «Согласиться» / «Отказаться»
  - Согласиться → **`commit_aftermath`**: через 3 месяца
    рутина утомляет команду. Закрыть досрочно (+350К, −fatigue,
    −rep) или дотянуть год (+750К, +18 fatigue).
  - Отказаться → **`decline_aftermath`**: через 3 мес.
    «Берёзка» в багах у конкурента — кейс «правильного нет»
    (+5 rep, +5 portfolio).

**📨 `tender_invite`** — «Приглашение в тендер от бренда»
*без requires*, minMonth 6, minPortfolio 5, cooldown 6, 3 стадии.
Тендер на 12М/год среди топовых сетевиков — **3 ветки**:

- Подать соло → `solo_resolve`: roll по менеджеру (+30%
  бонус), успех +1.8М/+8 rep, провал −120К/−1 rep.
- Подать консорциумом → `consortium_resolve`: гарантированные
  +900К/+5 rep, без рисков.
- Отказаться → end сразу (без затрат).

### Банк — +2 арки

**🚨 `data_breach`** — «Утечка персональных данных»
*без requires*, minMonth 6, minPortfolio 2, cooldown 8, 3 стадии.
В Telegram-канале опубликован дамп 12К записей карт:

- **leak** → 2 ветки:
  - «Признать публично» → **`apology`**: −500К компенсаций,
    +5 rep, +4 nudgeAll.
  - «Молчать» → **`silence`**: roll по юристу (+25% бонус),
    успех −2 rep (обошлись), провал −800К/−10 rep/−12 nudgeAll
    (штраф ЦБ + отток клиентов).

**🗺 `regional_expansion`** — «Заход в регион»
*requires `regulator_call` (продолжение арки v3.11)*, minMonth 12,
minRep 40, cooldown 8, 3 стадии. Губернатор предлагает льготы за
открытие филиала:

- **proposal** → 2 ветки:
  - «Открыть» (−400К на запуск) → **`open_result`**: roll по
    HR (+20% бонус), успех +800К/+6 rep/+10 portfolio/+1 лид,
    провал −250К/−4 rep.
  - «Подождать» → **`wait_result`**: конкурент захватил
    регион, +2 rep (не потратили зря).

### Архитектурное замечание

Все эффекты выборов — через тот же DSL `ScenarioLoader.applyOps`,
что и обычные события (`money`, `rep`, `fatigue`, `nudgeAll`,
`gAdd`, `staff`, `client`, `roll` с `roleBonus`, `log`, `notify`).
Никаких inline JS-функций в данных. Диспетчер цепляется через
обёртку `advanceMonth` + EventBus.emit('show_event') — тот же
модал #event-modal, что и v3.11.

### Валидация

- `sim/test-storyarcs.js` 37 → **67/67** (тесты 12–20: новые арки
  в пуле, requires-гейт по поведению, 3 ветки tender_invite,
  переходы по `next` на разные стадии, data_breach apology vs
  silence).
- Остальные тесты без изменений: `test-runmap` 108/108, `test-meta`
  85/85, `test-runes` 27/27, `test-live-switch` 25/25,
  `test-strategy` ✅, `test-outsource` 27/27.
- `sim2-lc agency 12` — 59 сдач / 4 ухода / 0 ошибок.
- `sim2-lc bank 4` — 23 сдачи / 5 уходов / 0 ошибок.
- `build multi` — 867.3 KB (+17.2 KB к v3.19, новые арки тяжёлые
  текстом).
- `build --scenario=bank` — 751.9 KB.

## v3.19 — Расширение этап-эксклюзивов Run Map (2026-06-15)

Покрытие этап-эксклюзивов выровнено по всем этапам обоих сценариев —
раньше у агентства эксклюзивы были только на двух последних этапах
(`studio_brand` / `studio_endgame`), у банка пусто было на стартовом
`bank_license`. Теперь milestone-модал на любой границе предлагает
не просто универсальные «сухарики», а 2–3 тематических варианта,
завязанных на смысле текущего этапа: ранний бутстрап даёт грант
и сарафан, команда — командный дух и стандарты, рост — медиа-кейс
и стратегического партнёра.

**Тип архитектуры: C** — только данные (`scenarios/<id>.data.js`),
`src/runmap.js` уже умеет фильтровать по `stages: [...]` (v3.17).
Никаких правок ядра.

### Что добавлено

**Агентство (`scenarios/agency.data.js`)** — +6 эксклюзивов
(2 на каждый из трёх «голых» этапов):

- 🏚 **studio_garage**:
  - 🔥 **Сарафанное радио** — `+1 caseScoutBonus`, `+2 rep`
    (первые довольные клиенты рекомендуют)
  - 💰 **Грант от акселератора** — разовая `+180 000 ₽`
- 🛠 **studio_team**:
  - 🤝 **Командный дух** — `−10 fatigue`, `+5% perkRecoveryBonus`
    (команда сплочённая после первых побед)
  - 📋 **Стандарты процесса** — `+5% speedUpgrades`, `+3 caseQBonus`
- 📈 **studio_growth**:
  - 📺 **Кейс в отраслевых медиа** — `+8 portfolio`, `+3 rep`
    (Cossa/AdIndex)
  - 🎯 **Стратегический партнёр** — `+1 caseScoutBonus`,
    `+5% perkPrepayBonus` (постоянный заказчик)

Итого в банке агентского пула: 12 универсальных + 10 эксклюзивов
(по 2 на каждый из 5 этапов) = **22 бонуса**.

**Банк (`scenarios/bank.data.js`)** — +3 эксклюзива:

- 📜 **bank_license** (раньше эксклюзивов не было):
  - 💼 **Госконтракт по обслуживанию** — разовая `+250 000 ₽`,
    `+2 rep` (выигран бюджетный тендер)
  - 🪙 **Льготы как новому банку** — `−15% overhead до конца партии`,
    `+3 rep` (стартовые льготы ЦБ)
- 👑 **bank_topten** (было 2, стало 3):
  - 🏭 **M&A: поглощение регионального банка** —
    разовая `+500 000 ₽`, `+15 portfolio`

Итого банковского пула: 12 универсальных + 11 эксклюзивов
(license 2, retail 2, corp 2, private 2, topten 3) = **23 бонуса**.

### Архитектурное замечание

Балансовая дифференциация: новые эксклюзивы сохраняют принцип
«эксклюзивы не запираемые» — их нет в `BONUS_UNLOCKS`, мета-фильтр
`RogueMeta.isBonusUnlocked` их не трогает. Это значит, что на 0 ✦
ранний игрок уже видит тематические варианты, не только базовый
набор `cash/rep/payout/overhead/cases/scout/rep_recovery/speed`.
Запираемые остаются те же 4 (`prepay/penalty_shield/fatigue/portfolio`).

Эффекты эксклюзивов выровнены по силе с универсальными аналогами:
например, `process_standards` даёт `+5% speedUpgrades + 3 caseQBonus`
— чуть слабее `boutique_premium` (`+10% perkPayoutMult`) ради
сохранения прогрессии «ранний этап даёт меньше, поздний больше».

### Валидация

- `sim/test-runmap.js` 83 → **108/108** (новые блоки: 16 — агентство
  studio_garage/team/growth эксклюзивы видны на своих и не видны на
  чужих; 17 — банк bank_license эксклюзивы + новый bank_topten;
  18 — milestone studio_garage реально предлагает только подходящие
  studio_team бонусы при переходе). Тест 12 (banking — размер пула):
  20 → 23.
- `sim/test-meta.js` 85/85 (тест 13 обновлён: пул агентства 16 → 22,
  открытых при 0 ✦ 12 → 18).
- Остальные тесты без изменений: `test-runes` 27/27, `test-storyarcs`
  37/37, `test-live-switch` 25/25, `test-strategy` ✅, `test-outsource`
  27/27.
- `sim2-lc agency 12` — 67 сдач / 0 ошибок (с v3.18 было 47 → новые
  бонусы не ломают флоу, наоборот, дают больше variance).
- `sim2-lc bank 4` — 22 сдачи / 1 уход по NPS / 0 ошибок.
- `build multi` — 850.1 KB (с v3.18 845.9 KB; +4.2 KB на новые
  данные).
- `build --scenario=bank` — 744.1 KB.

## v3.18 — Live-переключение сценария и сложности без перезагрузки (2026-06-14)

Переключение сценария (агентство / банк) и уровня сложности
(easy / normal / hard / nightmare) на mode-screen теперь работает
**без `location.reload`**. Раньше каждый клик по карточке вызывал
полный перезапуск страницы — мы теряли логи DLC, заново гоняли
инициализацию EventBus, дёргали загрузчик в верхнем уровне HTML.
Теперь это in-place переключение за один тик: подменили SCENARIO,
переприсвоили биндинги движка, перерисовали mode-screen.

- **`src/engine.js`** — 8 `const`-биндингов (`STAFF_DEFS`/`STAFF_ROLES`/
  `ROLE_LABELS`/`PROJECT_POOL`/`BUDGET_RANGES`/`UPGRADES`/`SPECS`/
  `EVENTS`) превращены в `let`. Раньше они держали ссылки на старый
  сценарий после reload, и для смены требовался перезапуск страницы.
  Новая функция `rebindFromScenario()` — единая точка
  перепривязки всех биндингов под текущий глобальный `SCENARIO`
  (8 алиасов + 4 экономические настройки overhead/actionsPerMonth/
  scoutCost/hireCost). `initState()` вызывает её первой строкой —
  работает и при первом запуске, и после live-смены.
- **`src/ui.js` — `switchScenarioLive(id)`**:
  1. Если активна партия (`G.month > 0`) — `confirm` про сброс,
     при отмене ничего не делаем.
  2. Сбросили партию (`resetGame()`) и навигируем на mode-screen.
  3. `localStorage.setItem('bt_scenario_v1', id)`.
  4. Загрузили SCENARIO_DATA для нового сценария:
     - **multi-дист**: через `new Function(window.__SCEN_SRC[id])()`
       (карта id → исходник теперь экспортируется в `window.__SCEN_SRC`,
       см. `build/build.js`).
     - **dev**: динамический `<script src="scenarios/<id>.data.js?ts=…">`
       (`?ts` обходит кеш браузера).
  5. `SCENARIO = ScenarioLoader.hydrate(window.SCENARIO_DATA)` →
     `SE.applyActiveScenario()` → `rebindFromScenario()` → `initState()`.
  6. Перерисовали mode-screen и интро (`initScenarioSelect`,
     `initDifficultySelect`, `renderSpecGrid`, `applyScenarioChrome`,
     `SE.syncIntroStats`), обновили состояние кнопки «Продолжить».
  7. `notify` с иконкой нового сценария.
  8. На любом фатале — фолбэк на `location.reload()`, чтобы хуже
     старого поведения точно не стало.
  Старая функция `switchScenario(id)` сохранена как алиас на live —
  back-compat для прямых вызовов из inline `onclick`.
- **`src/ui.js` — `switchDifficultyLive(id)`** — аналогично, но проще:
  `ScenarioLoader.applyDifficulty(SCENARIO, id)` мутирует
  `SCENARIO.settings` под новый пресет → `rebindFromScenario()` →
  `initState()` → перерисовка карточек сложности и интро.
  Старая `switchDifficulty(id)` теперь алиас.
- **`build/build.js`** — в multi-дисте `__SCEN_SRC` объявляется как
  `window.__SCEN_SRC` (раньше `var __SCEN_SRC`, был доступен только
  внутри блока хайдинга). Из `switchScenarioLive` доступ к карте
  исходников через `window.__SCEN_SRC[id]` — для дев и дист
  одинаковый код.
- **Валидация**:
  - `sim/test-live-switch.js` (новый) — **25/25**: `rebindFromScenario`
    есть и работает идемпотентно (4 повторных вызова не ломают);
    live-смена agency → bank в одном vm-контексте через
    `new Function(BANK_DATA_SRC)` обновляет `SCENARIO.id`, `SPECS`
    (agency-spec `smm` исчезает), `PROJECT_POOL`, `UPGRADES`,
    `EVENTS`, `OVERHEAD`; после `selectSpec` + `startGame` —
    `G.money` и `G.reputation` берут стартовые значения нового
    сценария; live-смена сложности через `applyDifficulty`
    меняет overhead/winCondition/startMoney под пресет (easy:
    overhead × 0.75, startMoney × 1.4, winCondition × 0.7;
    hard вернулся выше).
  - test-meta 85/85 · test-runes 27/27 · test-storyarcs 37/37 ·
    test-runmap 83/83 · test-strategy 23/23 · test-outsource 27/27
  - sim2-lc 12 agency: 0 ошибок, 42 сдачи · sim2-lc 4 bank: 0 ошибок, 20 сдач
  - build multi/agency/bank — OK
- **Тип архитектуры**: правка ядра (`engine.js` const→let + новая
  функция) + правка UI (`ui.js`) + правка build. Godot-портируемость
  сохранена — `rebindFromScenario` в GDScript будет одной функцией
  с присваиваниями полей синглтона; новая логика DSL-эффектов
  не затронута.
- **UX-улучшения**:
  - Без вспышки белого экрана при смене сценария/сложности.
  - Логи DLC и счётчик ранов не теряются между переключениями.
  - Если посреди партии случайно кликнули на другой сценарий —
    `confirm` спросит перед сбросом (раньше reload сбрасывал
    без подтверждения).


## v3.17 — Этап-эксклюзивные бонусы Run Map (2026-06-14)

Ещё один под-шаг п.13 — **сценарные milestone-арки**: бонусы Run Map
теперь могут быть **привязаны к конкретным этапам** через декларативное
поле `stages: [...]` в данных сценария. Это позволяет каждому этапу
иметь свою тематику — например, на финале банка предлагаются «SPO» и
«СЗКО-статус», которых не было раньше.

- **Поле `stages` / `exclusiveStages`** в объекте бонуса (опциональное):
  - `stages: ['bank_corp']` — бонус показывается **только** на этом этапе
  - Без поля — бонус универсальный (старое поведение, back-compat)
- **`src/runmap.js` v0.2 → v0.3** — фильтрация в `_showMilestoneModal`:
  - Сначала отбираем универсальные + подходящие текущему этапу.
  - Затем мета-фильтр (`RogueMeta.isBonusUnlocked`).
  - Фолбэк 1: добираем запертыми этапно-подходящими.
  - Фолбэк 2: добираем только универсальными (этап-эксклюзивы НЕ
    участвуют в фолбэке — гарантия что эксклюзивы появляются **только**
    на своём этапе).
  - Гарантия 3 вариантов сохранена.
  - Новый публичный API: `bonusFitsStage(b, stageId)` и
    `getBonusesForStage(stageId)`.
- **Банк (`scenarios/bank.data.js`) — 8 этап-эксклюзивов**, по 2 на этап:
  - 🏦 **bank_retail**: 💾 Ядро АБС (+400K) · 📱 Моб.приложение (+6 реп)
  - 🏢 **bank_corp**: 💱 Валютный деск (+7% выплат) · 🏗 Синдикаты (+1 лид · +5 Q)
  - 💎 **bank_private**: 🥂 VIP-офис (+10% выплат) · 🧑‍💼 Wealth-advisors (+10 портф. · +1 реп/мес)
  - 👑 **bank_topten**: 🏛 SPO (+800K) · 🌐 СЗКО-статус (penalty shield + 5 реп)
- **Агентство (`scenarios/agency.data.js`) — 4 этап-эксклюзива**:
  - 🏛 **studio_brand**: 🎤 Thought leadership (+1 лид · +1 реп/мес) ·
    🏅 Отраслевые награды (+15 портф. · +5 реп)
  - 👑 **studio_endgame**: 💎 Бутик (+10% выплат) · 🌐 Франшиза (+600K)
- **Валидация**:
  - `sim/test-runmap.js` 55 → **83/83**: новые блоки на
    `bonusFitsStage`/`getBonusesForStage`, milestone на bank_retail
    предлагает только retail-подходящие, агентский studio_brand
    показывает brand-эксклюзивы и не показывает endgame, и наоборот.
    Существующий тест на «у банка 12 бонусов» обновлён под новые
    20 (12 универсальных + 8 эксклюзивов).
  - `sim/test-meta.js` обновлён: пул агентства 12 → 16 (+4 эксклюзива),
    открытых при 0 ✦ 8 → 12. 85/85 зелёные.
  - sim2-lc 12 agency: 0 ошибок, 60 сдач · sim2-lc 4 bank: 0 ошибок, 15 сдач
  - test-runes 27/27 · test-storyarcs 37/37 · test-strategy 23/23 ·
    test-outsource 27/27 · build multi/agency/bank — OK
- **Тип архитектуры**: B (правка runmap.js) + C (данные сценариев).
  Godot-портируемость сохранена: новое поле `stages` — чистый JSON,
  фильтр на стороне интерпретатора.
- **Что осталось по п.13**: полноценный выбор второй спеки для
  «Серийный предприниматель» (инвазивно), расширение контента арок
  (ветвящиеся графы, кросс-арочные `requires`), балансовая
  дифференциация банковских бонусов (универсальные пока зеркалят
  агентство, можно тюнить), модификаторы сложности на ✦, мета-руны.


## v3.16 — Расширение мета-прогресса (2026-06-14)

Ещё один под-шаг п.13 — **расширение мета-прогресса**: новые ачивки,
визуальный прогресс-бар до следующей разблокировки, трекинг пиков
money в течение партии. Полностью внутри `src/meta.js` v0.1 → v0.2,
ядро не модифицировано.

- **+6 ачивок** (5 новых event-based + 1 коллекционная):
  - 💰 **Миллионщик** (+150 ✦) — пик money ≥ 5 000 000 ₽ за партию.
    Использует трекер `_runMaxMoney` через обёртку `advanceMonth`
    (фолбэк на `g.money`).
  - 🎲 **Коллекционер рун** (+200 ✦) — все 4 базовые руны
    (`connections`/`perfectionist`/`insider`/`serial`) хотя бы по разу.
    Хранит `meta.playedRuneIds[]` отдельно от history (история обрезается).
  - ⚡ **Скоростной ран** (+150 ✦) — победа на M ≤ 20.
  - 🔥 **Феникс** (+100 ✦) — победа после провала до ≤ 50 000 ₽
    (трекер `_runMinMoney`).
  - 🛡 **Без срывов** (+80 ✦) — финал с репутацией ≥ 80.
  - 🔄 **Серия из трёх** (+250 ✦) — три победы подряд (без банкротств
    между ними). Через `lastN(3)`.
- **Геттеры прогресса** (новые в публичном API):
  - `getNextRuneUnlock()` → `{ id, shards, remaining }` ближайшей
    запертой руны (или `null` если все открыты).
  - `getNextBonusUnlock()` → аналогично для бонусов Run Map.
- **UI**:
  - Кнопка «⚡ Мета-прогресс» на mode-screen теперь двухстрочная:
    верх — название + текущие `N ✦`, низ — прогресс-бар (золотой)
    с подписью «Следующая разблокировка: 🔒 hardened — ещё X ✦».
  - Шапка модала показывает строку «🔒 руна `id` — ещё X ✦ · 🔒 бонус
    `id` — ещё Y ✦» и счётчик «собрано рун: K/4».
  - Каждая запертая строка в списках рун/бонусов теперь с мини
    прогресс-баром (cyan/violet) и подписью `M / N ✦ · −R`.
- **Трекинг money** через обёртки в `meta.js`:
  - `startGame` → инициализирует `G._runMaxMoney` / `G._runMinMoney`
    стартовым money.
  - `advanceMonth` (после оригинального вызова) → обновляет min/max.
  - Engine не модифицирован, всё через подмену глобальных функций.
- **Стейт** расширен:
  - `meta.playedRuneIds[]` — копится между ранами для коллекционера.
  - `run.peakMoney` / `run.lowestMoney` / `run.finalReputation`
    добавлены в каждую запись `history`.
- **Тип архитектуры**: B (часть кастомной версии Rogue-lite, гейтуется
  тем же DLC).
- **Валидация**:
  - `sim/test-meta.js` 57 → **85/85**: добавлены кейсы — геттеры
    `getNextRuneUnlock`/`getNextBonusUnlock` (порог hardened=150,
    prepay=100), millionaire (срабатывает на 5.5M, не на 800K),
    rune_collector (все 4 руны → ачивка на 4-й), speedrun (M18 да,
    M22 нет), phoenix (lowestMoney 30K + победа), no_breakdowns
    (реп 85 vs 65), win_streak_3 (3 победы подряд → срабатывает;
    банкрот посередине разрывает серию), peakMoney/lowestMoney
    инициализация через `startGame` и обновление через `advanceMonth`.
  - Тест 3 обновлён: первая победа на 9M теперь даёт 540 ✦
    (было 390 + millionaire 150).
  - sim2-lc 12 agency: 0 ошибок, 62 сдачи · sim2-lc 4 bank: 0 ошибок, 23 сдачи
  - test-runes 27/27 · test-storyarcs 37/37 · test-runmap 55/55 ·
    test-strategy 23/23 · test-outsource 27/27 · build multi/agency/bank — OK
- **Что осталось по п.13**: полноценный выбор второй спеки для
  «Серийный предприниматель» (инвазивно — расширение SPECS-аксессоров),
  расширение контента арок (ветвящиеся графы, кросс-арочные `requires`),
  балансовая дифференциация банковских бонусов Run Map, сценарные
  milestone-арки (этап → разный пул бонусов), визуальный progress hint
  для конкретных ачивок (полоска прогресса под названием).


## v3.15 — Run Map в данных сценария (2026-06-14)

Закрыт ещё один под-шаг п.13 — **Run Map переехала в данные сценария**
(Тип C по правилу архитектуры). Этапы и пул бонусов теперь описываются
в `scenarios/<id>.data.js → runMap: { stages: [...], bonuses: [...] }`,
бонусы — чистые данные через DSL `effects[]`, без inline JS-функций.
Банк получил свою тематическую карту.

- **`SCENARIO.runMap`** — новая опциональная секция данных сценария
  с двумя полями:
  - `stages[]` — этапы рана (`id`, `name`, `icon`, `color`, `monthEnd`, `sub`)
  - `bonuses[]` — пул milestone-бонусов (`id`, `icon`, `name`, `desc`,
    `effects[]` в DSL scenario-loader, `log` для записи)
- **Расширение DSL applyOps** (`src/scenario-loader.js`):
  - `gAdd: { ключ: число }` — складывает дельты с G-каналами
    (`perkPayoutMult`, `caseQBonus`, `caseScoutBonus`, `caseRepBonus`,
    `speedUpgrades`, `perkPrepayBonus`, `perkRecoveryBonus`, `portfolio`).
    Округление: 2 знака для дробных, целое для `money`/`portfolio`/`reputation`.
  - `gSet: { ключ: значение }` — присваивание (для bool-флагов вроде
    `perkPenaltyShield`).
  - `overheadBump: число` — добавка к `runeOverheadBump` пропорционально
    `SCENARIO.settings.overhead`. Отрицательное значение → экономия
    (применяется в `_postAdvance` модуля рун).
- **`src/runmap.js` v0.1 → v0.2**:
  - Дефолтные `STAGES` и `BONUSES` → `DEFAULT_STAGES` / `DEFAULT_BONUSES`,
    у бонусов inline `apply(g)` заменён на `effects[]` (back-compat
    legacy `apply` сохранён в `_applyBonusEffects` фолбэком).
  - Чтение через `_getStages()` / `_getBonuses()`: предпочитаем
    `SCENARIO.runMap.{stages,bonuses}`, фолбэк — встроенные дефолты.
    Проверка минимума: stages ≥ 2, bonuses ≥ 3.
  - Эффекты применяются через `ScenarioLoader.applyOps(b.effects, g)`.
  - Публичный API расширен: `getDefaultStages()` / `getDefaultBonuses()` /
    `applyBonusEffects(b, g)`.
- **`scenarios/agency.data.js`** — добавлен `runMap`-блок:
  5 этапов (🏚 Гараж-студия → 🛠 Команда сложилась → 📈 Заметное агентство →
  🏛 Бренд → 👑 Топ-лист) с агентскими `id` (`studio_garage`..`studio_endgame`),
  12 бонусов через DSL.
- **`scenarios/bank.data.js`** — собственный тематический `runMap`:
  - 5 этапов: 📜 Получили лицензию → 🏦 Розничная сеть → 🏢 Корпоративный
    портфель → 💎 Private banking → 👑 Топ-10
  - 12 банковских бонусов: 🐖 Депозитная база · ⭐ Доверие клиентов ·
    📈 Маржинальные продукты · 🤖 Автоматизация бэк-офиса · 🧠 Скоринг-модель ·
    🏪 Сетка отделений · 📣 Служба коммуникаций · 🚀 RPA-команда ·
    💳 Усиленный юр-блок · 🛡 Лобби в регуляторе · 🌿 Программа благополучия ·
    📚 Премии за кейсы.
  - Те же эффекты что в агентстве — тематика только в названиях/описаниях,
    балансово одинаково (можно тюнить отдельно если понадобится).
- **Тип архитектуры**: C (данные сценария) — никакого нового кода в ядре,
  все различия в `<id>.data.js`. Godot-портируемость сохранена: эффекты
  читаются как JSON, интерпретатор `applyOps` переносится 1-в-1.
- **Валидация**:
  - `sim/test-runmap.js` 32 → **55/55**: добавлены кейсы про bank
    (свои этапы и бонусы, нет агентских id), про DSL-эффекты
    (`money`/`gAdd`/`gSet`/`overheadBump` реально мутируют G), про фолбэк
    на `DEFAULT_STAGES`/`DEFAULT_BONUSES` при стёртом `SCENARIO.runMap`.
    Существующие тесты обновлены под новые id агентства
    (`startup` → `studio_garage`) и под `effects[]` вместо `apply()`.
  - `sim2-lc 12 agency` 0 ошибок, 51 сдача · `sim2-lc 4 bank` 0 ошибок, 21 сдача
  - `test-meta` 57/57 · `test-runes` 27/27 · `test-storyarcs` 37/37 ·
    `test-strategy` 23/23 · `test-outsource` 27/27
  - `build` multi/agency/bank — OK
- **Что осталось по п.13**: полноценный выбор второй спеки для
  «Серийный предприниматель» (требует расширения SPECS-аксессоров),
  расширение контента арок (ветвящиеся графы, кросс-арочные `requires`),
  балансовая дифференциация банковских бонусов (сейчас зеркалят агентство),
  расширение мета-прогресса (модификаторы сложности на ✦,
  визуальный прогресс до следующей разблокировки).


## v3.14 — Мета-прогресс между партиями (2026-06-14)

Закрыт четвёртый шаг п.13 — **мета-прогресс**: накопление осколков (✦)
между ранами, разблокировка новых стартовых рун и бонусов Run Map, журнал
ачивок. Активируется только при включённом DLC «Rogue-lite». Реализация —
опциональный модуль `src/meta.js`, ядро не модифицировано.

- **Стейт** хранится в `localStorage['bt_roguelite_meta_v1']` отдельным ключом —
  переживает перезагрузку, сброс партии и даже сброс сейвов. Структура:
  `{ shards, totalRuns, wins, achievements[], history[] }` (история обрезается
  до 50 последних ранов).
- **Начисление по результату рана** (хук `end_game` в `dlc/roguelite/roguelite.js`):
  база 100 ✦ за победу / 30 ✦ за поражение, +20 ✦ за каждый достигнутый этап
  Run Map (cap 80) + награды за впервые получаемые ачивки. Сводка через
  существующий `addLog` + `notify`.
- **Запираемые руны** (новые в пуле `src/runes.js`):
  - 🛡 **Закалённый** — 150 ✦: penalty shield + overhead +10%
  - 🎓 **Стипендиат** — 300 ✦: +10 Q, выплаты −5%
  - 🌐 **Сетевик** — 500 ✦: +2 лида, старт −300K ₽
  - 🌪 **Аутсайдер** — 750 ✦: overhead −20%, репутация −15
  - Расширенные каналы эффектов в `_applyRuneToG`: `qualityBonus`,
    `startReputationDelta`, `penaltyShield`; поддержка отрицательного
    `overheadBumpPct` (экономия в `_postAdvance`).
- **Запираемые бонусы Run Map** (`src/runmap.js`):
  - 💳 Юр-бренд — 100 ✦, 🛡 Авторитет — 200 ✦, 🌿 Велнес — 400 ✦,
    📚 PR-кампания — 600 ✦.
  - Фильтрация пула в `_showMilestoneModal` через `RogueMeta.isBonusUnlocked`
    + фолбэк-гарантия 3 вариантов.
- **8 ачивок** с уникальными иконками (one-shot, не повторяются): первый ран
  (+50), первая победа (+100), до эндгейма (+60), победа с конкретной руной
  (3 × +50), мастер агентства / 5 побед (+200), выживший / 3 рана без
  банкротства (+75).
- **UI**: кнопка «⚡ Мета-прогресс Rogue-lite [N ✦]» на mode-screen ниже
  карточек DLC (через `_injectModeButton`). Модал — три блока: ачивки
  (грид с иконками-серостью на нереализованных), таблицы рун/бонусов
  (зелёные открытые / 🔒 запертые с порогом), последние 5 ранов из истории.
  Сброс мета-прогресса через `confirm`.
- **Архитектура**: модуль гейтуется DLC «Rogue-lite» так же, как руны/арки/
  карта (прямое чтение `localStorage['bt_enabled_dlcs_v1']`, объект `DLC`
  на момент парсинга недоступен). Hard kill-switch `META_ENABLED`.
  Координатор DLC `dlc/roguelite/roguelite.js` обновлён до v0.4: добавлен
  четвёртый компонент `meta`, в обработчике `end_game` вызывается
  `RogueMeta.awardAtEndGame(won, G)`, кнопка мета-прогресса перерисовывается
  для актуализации `N ✦`.
- **Тип архитектуры**: B (кастомная версия игры, как уточнено в
  `[[feedback-dlc-modularity]]` v3.12) — модуль физически в `src/` ради
  single-HTML build, активация только через DLC.
- **Валидация**:
  - `sim/test-meta.js` — **57/57**: API, дефолт, начисление shards, ачивки
    `first_run`/`first_win`/`endgame_reached`/`master_5_wins`/`survivor`/
    `social_win`, разблокировки по порогам (150 → hardened, 100 → prepay),
    обрезание истории до 50, `reset()`, kill-switch, без DLC, persistence
    через localStorage (новый sandbox видит старые shards), интеграция
    с runes (4 заперты при 0 ✦) и runmap (4 бонуса заперты при 0 ✦).
  - `test-runes` 27/27 (тест 1 обновлён: пул 4 → 8 рун с
    учётом запираемых через мета-прогресс)
  - `sim2-lc 12 agency` 0 ошибок, 55 сдач · `sim2-lc 4 bank` 0 ошибок, 23 сдачи
  - `test-storyarcs` 37/37 · `test-runmap` 32/32 · `test-strategy` 23/23 ·
    `test-outsource` 27/27 · `build` multi/agency/bank — OK
- **Что осталось по п.13**: полноценный выбор второй специализации для руны
  «Серийный предприниматель» (требует расширения SPECS-аксессоров; инвазивно
  для engine — нужно решить как обойтись без правок ядра), расширение
  контента сюжетных арок (ветвящиеся графы, кросс-арочные `requires`),
  вынос этапов/пула Run Map в данные сценария (Тип C — банк со своими
  тематическими этапами и бонусами).


## v3.13 — Карта рана Run Map (2026-06-14)

Закрыт третий шаг п.13 — **Run Map**: партия делится на 5 этапов с
понятными иконками, на границах выскакивают milestone-модалы с выбором
1 из 3 случайных бонусов. Активируется только при включённом DLC
«Rogue-lite» (см. v3.12). Реализация — опциональный модуль `src/runmap.js`,
ядро не модифицировано.

- **5 этапов** с цветовой раскладкой и подсказкой:
  - 🏚 **Гараж** (M0–6) — первые сделки, поиск стиля
  - 🛠 **Закрепление** (M6–14) — команда и постоянные клиенты
  - 📈 **Рост** (M14–22) — старшие тиры, инвестиции в процессы
  - 🏛 **Зрелость** (M22–30) — бренд узнают, эпики берут с расчётом
  - 👑 **Эндгейм** (M30+) — выжимаем максимум
- **Пул из 12 бонусов** (3 случайных на каждый milestone, без повторов выбора):
  - 💸 Дивиденды +250K · ⭐ Узнаваемость +5 реп · 📈 Премиум-позиц. +5% выплат
  - 🛠 Оптимизация overhead −10% · 🏆 Резонансный кейс +5 Q · 🔍 Связи +1 лид
  - 💼 PR-агентство +1 реп/мес · 🚀 Акселератор +5% скорости команды
  - 💳 Юр-бренд +15% шанс предоплаты · 🛡 Авторитет (штраф просрочки −50%)
  - 🌿 Велнес (−15 усталости + 5% восст.) · 📚 PR-кампания +10 портфолио
- **Архитектура**: обёртка `advanceMonth` детектит переход через `monthEnd`
  текущего этапа, защита от двойного срабатывания (`milestonesShown[]`),
  модал через существующий `EventBus.emit('show_event')` (тот же модал
  #event-modal). Выбор бонуса применяет эффект через те же G-каналы, что
  и руны/кейсы/перки — engine читает их без правок.
- **Стейт**: `G.runMap = { stageIdx, choicesTaken[], milestonesShown[] }` —
  переносится через `_restore` без правок saves.js.
- **UI-пилюля** этапа в `.game-header .game-logo` (рядом с пилюлей руны)
  через подписку на `render`. Title содержит подсказку про этап.
- **Гейт по DLC «Rogue-lite»** + hard kill-switch `RUN_MAP_ENABLED` (как у
  рун/арок). Координатор `dlc/roguelite/roguelite.js` теперь индикатор
  трёх компонентов (руны/арки/карта), DLC версия 0.2 → **0.3**.
- **Валидация**:
  - `sim/test-runmap.js` — **32/32** (API/этапы/бонусы, milestone на M6,
    выбор применяет эффект, защита от дубля, эндгейм не триггерит milestone,
    snapshot переносит стейт, выключение без DLC, kill-switch)
  - `sim2-lc 12 agency` 0 ошибок, 42 сдачи · `sim2-lc 4 bank` 0 ошибок, 20 сдач
  - `test-runes` 27/27 · `test-storyarcs` 37/37 · `test-strategy` 23/23 ·
    `test-outsource` 27/27 · `build` multi/agency/bank — OK
- **Что осталось по п.13**: мета-прогресс между партиями (накопление очков,
  открытие новых рун/бонусов), полноценный выбор второй специализации для
  руны «Серийный предприниматель», расширение контента сюжетных арок
  (ветвящиеся графы, кросс-арочные `requires`).


## v3.12 — Roguelite-механики гейтуются DLC (2026-06-14)

По уточнению Романа: **все механики п.13 (roguelite) должны срабатывать только
при включённом DLC «Rogue-lite»** — это не базовый геймплей, а кастомная версия.
Перевёл стартовые руны (v3.10) и Story Arcs (v3.11) под гейт DLC без переноса
файлов (чтобы single-HTML build остался без отдельной папки `dlc/` рядом с дистом).

- **Гейт активации** в `src/runes.js` и `src/storyarcs.js`: на старте IIFE
  проверяет `localStorage['bt_enabled_dlcs_v1']` на наличие `'roguelite'`
  (чтение напрямую — глобальный объект `DLC` объявляется в `dlc/loader.js`
  позже по порядку, недоступен на момент парсинга наших модулей). Без DLC
  модуль молча не регистрируется: `window.Runes` / `window.StoryArcs`
  не появляются, обёртки `startGame`/`advanceMonth`/`_generateOffers`
  не ставятся — игра ведёт себя как будто файлов нет.
- **Hard kill-switch** (`RUNES_ENABLED` / `STORY_ARCS_ENABLED`) сохранён
  как override для дебага/A-B-тестов (false выключает даже при включённом DLC).
- **Координатор** `dlc/roguelite/roguelite.js` обновлён до v0.2: проверяет
  `window.__RUNES_LOADED` / `window.__SA_LOADED`, логирует какие компоненты
  активны, ведёт счётчик ранов через `EventBus.on('end_game')` под мета-прогресс.
  Хук `month_end`/`project_completed`/`game_started` из stub-версии убран —
  движок их не эмитит, на v0.2 нужен только `end_game`.
- **Реестр DLC** (`dlc/loader.js` REGISTRY[roguelite]): описание обновлено —
  «Стартовые перки-руны + сюжетные арки». Версия DLC 0.1 → 0.2.
- **Manifest** `dlc/roguelite/manifest.json` синхронизирован.
- **Симы**: `test-runes` и `test-storyarcs` теперь по умолчанию ставят
  fake-`localStorage` с включённым `roguelite` (иначе все ассерты «модуль
  работает» провалились бы). Добавлен флаг `noRoguelite:true` в sandbox-опции
  и **новые negative-тесты**: «без DLC — руны/арки не активируются» (тест 10
  для runes, тест 11 для arcs).
- **Поведение для пользователя**: по умолчанию DLC «Rogue-lite» выключен
  (тумблер на mode-screen → раздел «Расширения») — новые партии идут без рун
  и без арок. Чтобы включить — щёлкнуть тумблер и перезагрузить страницу.
- Валидация: `test-runes` **27/27** · `test-storyarcs` **37/37** ·
  `sim2-lc 12 agency` 0 ошибок, 54 сдачи · `sim2-lc 4 bank` 0 ошибок, 23 сдачи ·
  `test-strategy` 23/23 · `test-outsource` 27/27 · `build` multi/agency/bank — OK.


## v3.11 — Сюжетные арки Story Arcs (2026-06-14)

Закрыт второй шаг п.13 бэклога — **Story Arcs** (многоэтапные цепочки событий-диалогов).
Реализовано как **опциональный подключаемый модуль ядра** (`src/storyarcs.js`, Тип A —
флаг `STORY_ARCS_ENABLED`). Ядро не модифицировано — диспетчер цепляется обёрткой
`advanceMonth` и пушит арки через существующий `EventBus.emit('show_event')` (тот же модал,
что у обычных событий). Контент живёт **в данных сценария** (`scenarios/<id>.data.js`,
новое поле `storyArcs`), эффекты выбора применяются через **тот же DSL** scenario-loader,
что и обычные события (Тип C для контента).

- **Структура арки** (опционально в любом сценарии):
  - `trigger`: окно (`minMonth`/`maxMonth`), гейты (`minRep`/`minPortfolio`), требование
    другой арки (`requires`), вероятность (`chance`, дефолт 0.4)
  - `stageDelayMonths` (дефолт 1), `cooldown` (дефолт 4 мес. после завершения)
  - `stages[].choices[]`: те же `text`/`desc`/`effects` (DSL), плюс `next: 'stageId'` или
    `end: true`
- **Диспетчер `_tickArcs()`**:
  1. Активная арка → ждёт `nextMonth` → выстреливает стадию
  2. Кулдаун → декремент
  3. Иначе ищет подходящую арку по триггерам, ролит шанс, стартует первую стадию
- **Гидрация**: при первом тике пробегает по `SCENARIO.storyArcs`, привязывает
  `choice.fn = (G) => { ScenarioLoader.applyOps(effects, G); _advance/end; }`
- **Контент v0.1** — по 2 арки на сценарий (3 и 2 стадии):
  - **agency**: «Звонок от старого знакомого» (intro → meeting → aftermath; интро +
    переговоры + комиссия посреднику) · «Сигнал выгорания» (анонимная записка →
    разбор источника)
  - **bank**: «Звонок из регулятора» (внеплановая проверка ЦБ → итог) · «VIP-клиент
    уходит к конкуренту» (одностадийная дилемма)
- **Стейт в G**: `G.arcState = { completed: [], inProgress: null, cooldown: 0 }` —
  сохраняется через существующий `_restore` без правок saves.js.
- **Подключение** — пара строк: `<script src="src/storyarcs.js">` в `index.html`,
  `read('src/storyarcs.js')` в `build/build.js`. Выключение — флаг
  `STORY_ARCS_ENABLED=false` или комментарий.
- **Валидация**:
  - `sim/test-storyarcs.js` — **34/34** проверки (модуль, API, гидрация, fire,
    next/end-флоу, minMonth-гейт, кулдаун, restore-сейва, выключение флагом,
    подключение в bank-сценарии)
  - `sim2-lc 12` (agency) — 0 ошибок/софтлоков, 54 сдачи
  - `sim2-lc 4 --scenario=bank` — 0 ошибок, 25 сдач
  - `test-runes` 24/24, `test-strategy` 23/23, `test-outsource` 27/27
  - `build/build.js`, `--scenario=agency`, `--scenario=bank` — OK
- **Что осталось по п.13**: Run Map (этапы рана с milestone events), мета-прогресс
  между партиями, полноценный выбор второй специализации для руны «Серийный
  предприниматель».


## v3.10 — Стартовые перки-руны (2026-06-14)

Закрыт первый шаг п.13 бэклога — **Roguelite-механики**: стартовые перки-руны.
Реализовано как **опциональный подключаемый модуль ядра** (`src/runes.js`) по правилу
[[dlc-modularity]] (Тип A — базовый геймплей, не кастомная версия). Ядро игры
(`engine.js`/`staff.js`/`projects.js`/`ui.js`) не модифицировано — модуль цепляется
обёртками `startGame` / `advanceMonth` / `_generateOffers` и подпиской на `render`.

- **Пул из 4 рун** (модал показывает 3 случайные с 2 перебросами):
  - 🤝 **Связи в отрасли** — +1 лид при скаутинге на весь ран, старт −150 000 ₽
  - 🎯 **Перфекционист** — завершённые проекты +5% к выплате, штраф просрочки ×2
  - 🕵 **Инсайдер рынка** — в каждом скаутинге гарантирован 1 rare/epic-оффер, overhead +15%
  - 🚀 **Серийный предприниматель** — при 15 баллах портфолио открывается «вторая специализация»: единоразово +20% к выплатам (полноценный выбор второй спеки — следующая итерация)
- **Поток**: spec выбрана → «Начать дело» → обёртка `startGame()` перехватывает первый
  вызов → модал → выбор записывает `G.activeRune`, применяет эффекты, продолжает
  оригинальный `startGame()`. После рестарта рана (`resetGame`) `_pendingRune` чистится,
  следующий ран снова покажет модал.
- **Каналы эффектов** — пять штук, всё в G:
  - `startMoneyDelta` → разовое изменение `G.money` после старта
  - `scoutBonus` → `G.caseScoutBonus` (существующий канал бонусов лидов)
  - `payoutMult` → `G.perkPayoutMult` (существующий канал выплат)
  - `penaltyMult` → новый `G.runePenaltyMult` (читается обёрткой `advanceMonth`,
    добавляет ×N штраф репутации к базовому штрафу просрочки)
  - `overheadBumpPct` → новый `G.runeOverheadBump` (абсолютная сумма, списывается
    обёрткой `advanceMonth` после оригинального месяца — `let OVERHEAD` в engine.js
    недоступен из скоупа модуля, поэтому делаем доплату, а не подмену)
  - флаги `runeInsiderRare` / `runeSerialUnlock` / `runeSerialApplied` — для специфических
    эффектов отдельных рун
- **UI-инжект**: пилюля «🎲 Руна: <name>» вставляется в `.game-header .game-logo` через
  подписку на `EventBus.emit('render')`. Стиль — раскраска цвета руны (mint/violet/amber/cyan),
  hover-title показывает полное описание.
- **Сейвы**: `G.activeRune` сериализуется существующим `_restore` (он копирует все поля G),
  все эффекты-флаги тоже в G — сейвы переносятся без правок `saves.js`.
- **Включение/выключение — пара строк**: флаг `RUNES_ENABLED` в `src/runes.js` или
  комментирование `<script src="src/runes.js">` в `index.html` + строка
  `read('src/runes.js')` в `build/build.js` (`postEngineBlocks`).
- **Валидация**:
  - `sim/test-runes.js` — **24/24** проверки (API доступно, каждая руна применяется
    корректно, обёртки `advanceMonth`/`_generateOffers` работают, `RUNES_ENABLED=false`
    полностью отключает модуль);
  - `sim2-lc 12` (agency) — 0 ошибок/софтлоков, 53 сдачи;
  - `sim2-lc 4 --scenario=bank` — 0 ошибок, 23 сдачи;
  - `test-strategy` — 23/23, `test-outsource` — 27/27;
  - `build/build.js`, `--scenario=agency`, `--scenario=bank` — OK (модуль в пайплайнах).
- **Что осталось по п.13** (отдельными итерациями): Story Arcs (цепочки событий),
  Run Map (этапы рана), мета-прогресс между партиями, полноценный выбор второй
  специализации для Серийного предпринимателя.


## v3.9 — Уровни сложности (2026-06-14)

Закрыт п.4 бэклога — пресеты экономики **Easy / Normal / Hard / Nightmare** с выбором
до старта. Пресеты хранятся в `src/scenario-loader.js` (`DEFAULT_DIFFICULTIES`),
сценарий может переопределить через `data.difficulties` — общая шкала не дублируется
в каждом `<id>.data.js`.

- **Моды** применяются к `settings` ДО того, как `engine.js` биндит `let`-константы:
  - `easy` — старт ×1.4, overhead ×0.75, цель ×0.7, реп +10
  - `normal` — эталон (моды пустые)
  - `hard` — старт ×0.8, overhead ×1.35, цель ×1.2, реп −10
  - `nightmare` — старт ×0.55, overhead ×1.7, цель ×1.4, реп −20
- **UI**: новый блок «Сложность» на mode-screen (`#difficulty-select`) — 4 карточки с
  тёплыми/холодными цветами по тиру (зелёный / тил / янтарь / алый), список перков
  пилюлями, активный пресет подсвечивается. Хранение `localStorage['bt_difficulty_v1']`,
  переключение перезагружает страницу (`switchDifficulty(id)` — по аналогии со сценарием).
- **Intro-экран**: новая пилюля «Сложность: <label> — <desc>» под интро-текстом; число
  цели в `introText` синхронно подменяется на актуальный `winCondition` после мода.
- **Симы**: `sim2-lc.js` принимает `--difficulty=easy|normal|hard|nightmare` (через
  fake-`localStorage`). Прогон 8×4 (агентство): easy 4 победы / 1 банкрот · normal 2/1 ·
  hard 0/3 · nightmare 0/6 — монотонная шкала, 0 ошибок/софтлоков на всех пресетах.
- Валидация: `node --check` × 4 файла; `sim2-lc 12` (agency, normal) — 0 ошибок;
  `sim2-lc 4 --scenario=bank` — 0 ошибок; `test-strategy` — 23/23; `test-outsource` — 27/27;
  `build/build.js`, `--scenario=agency`, `--scenario=bank` — OK.
- **Совместимость**: ядро не тронуто, фича чисто в `scenario-loader.js` + `ui.js` + UI-инжект
  на mode-screen. Соответствует [[godot-portability]] (всё в данных и UI, в `engine.js` —
  ноль строк) и допустима в ядре по правилу [[dlc-modularity]]: выбор сложности — часть
  базового геймплея, не «крупная фича», требующая отдельного модуля.


## v3.8 — Подрядчики (аутсорс), опциональный модуль (2026-06-13)

Закрыт п.3 бэклога фич — «Аутсорс-ресурс». По уточнённому решению Романа реализован
не как DLC, а как **опциональный подключаемый модуль ядра** (`src/outsource.js`),
который выключается/включается одной строкой (комментарий `<script>` в `index.html`
или флаг `OUTSOURCE_ENABLED` в самом файле). Ядро игры (`engine.js`, `staff.js`,
`projects.js`, `ui.js`) не модифицируется — модуль цепляется обёртками.

- **3 типа подрядчиков** (контракт фикс. на 3–4 мес., оплата разовая авансом):
  - 🏢 **Студия-партнёр** — Q +9, V +5, ⚙ senior · 3 мес · 480K ₽
  - 🛰 **Контракт-копирайтеры** — V +30, Q +7 · 3 мес · 360K ₽
  - 📡 **Внешний PM** — слот +1, Q +6, V +4 · 4 мес · 620K ₽
- **Включение/выключение — пара строк кода**:
  - выключить целиком → `OUTSOURCE_ENABLED = false` в `src/outsource.js` (или убрать `<script src="src/outsource.js">` из `index.html` и одну строку из `postEngineBlocks` в `build/build.js`)
  - включить обратно → вернуть строку. Стейт-маркеры на сейвах при отключении просто игнорируются (модуль не подменяет ничего, обёрток нет).
- **Изоляция**: подрядчик кладётся в `G.staff` с маркером `s._outsource = {monthsLeft, totalMonths, paid, type}`, `salary=0` (контракт оплачен авансом → не учитывается в `getTotalStaffCost`), `loyalty=100, mood=100, traits=[]` — не накапливает усталость и не уходит сам.
- **Обёртки вместо правок ядра**: `advanceMonth` (декремент monthsLeft + авто-`unassignStaff` + удаление по истечении), `renderTeamCards` (бейдж «🤝 контракт N/M мес.» + скрытая кнопка ✕ — выходного пособия у подрядчика нет, цветовая рамка по типу).
- **UI-инжект**: кнопка «🤝 Подрядчики» в панели «Действия» рядом со скаутингом + полоски остатка контракта; модал найма с тремя карточками-предложениями, разовая оплата и блок-стейт при недостатке средств.
- **Build/dev**: подключение через `<script src="src/outsource.js">` в `index.html` и `read('src/outsource.js')` в `postEngineBlocks` (`build/build.js`); попадает в multi и `--scenario=*` дист одинаково.
- Валидация: `sim/test-outsource.js` — **27/27** проверок зелёные (контроль API, оплаты, throughput/Q/V/capacity, декремент срока, авто-отвязка от проекта, salary≠ФОТ, мульти-контракты одного типа); `sim2-lc 12` (агентство) — 0 ошибок/софтлоков, 54 сдачи; `sim2-lc 4 --scenario=bank` — 0 ошибок; `test-strategy` — 23/23; build чист (multi и bank, модуль встроен).


## v3.7 — Прогрессивный кэп эффективности (2026-06-13)

Жёсткий кэп ×1.5 (v2.4) заменён прогрессивной отдачей — по итогам A/B/C-теста
(24 рана/режим, sim2-lc `--eff-cap`): hard 25% побед · **soft 42%** · none 37%
(но темп схлопывался до M19).

- **`effFromRatio(r)` в engine.js** — единая точка правды: до 100% нагрузки линейно,
  сверх — `1 + √(r−1)×0.5`. Перевыполнение ×2 → 150%, ×4 → 187%, ×7 → 222%.
  Распределение людей остаётся осмысленным (отдача убывает), темп партии не схлопывается
- **Все 7 точек формулы** переведены на effFromRatio: advanceMonth, превью карточки (_pace),
  строка команды, assign-модал, прогноз P&L (forecastInflows), календарь (_projectPaceMonths)
- **Подписи кэпа переписаны**: «150% макс» → «убыв. отдача»; «избыток N ед. без эффекта» →
  «⚠ Перевыполнение ×N — прирост минимален (√-отдача), людей выгоднее перевести» (при ×3+);
  шкала бара эффективности 150% → 220%
- **sim2-lc**: дефолт `--eff-cap` теперь soft (= движок), hard/none подменяют тело effFromRatio
- Попутно (коммит 98526c4): строка темпа «этап ~X мес. · проект ~Y мес.» — прогнозы
  фазы и всего проекта разделены
- Валидация: agency 12 ранов (победы, 0 ошибок), bank, strategy-тест 23/23 — чисто


## v3.3 — Производственный календарь рабочих дней (2026-06-12)

Ресурс «10 дней в месяце» шёл вразрез с реальностью. Выбран путь реализма:

- **`settings.workdays[12]`** в данных сценария — реальное число рабочих дней по производственному календарю: Янв 17 · Фев 20 · Мар 21 · Апр 22 · Май 18 · Июн 21 · Июл 23 · Авг 21 · Сен 22 · Окт 23 · Ноя 20 · Дек 22. Движок: `getWorkdays(monthIdx)`, фолбэк — плоский `actionsPerMonth` (теперь 21) для кастомных сценариев без календаря
- **Стоимости действий ×2** (пропорции сохранены): скаутинг 3→6 дней, найм 2→4, апгрейды 1–3→2–6, сборка кейса 1/2/3→2/4/6 (грейд-формула пересчитана)
- **Побочный геймплей-бонус — сезонность**: короткие январь (17) и май (18) реально ощущаются — действий меньше; длинные июль/октябрь (23) — окно для рывка
- UI: пипсы и счётчик «X / Y» — по календарю текущего месяца
- Валидация: симы агентства/банка и strategy-тест — чисто, баланс не сдвинулся (средний ресурс 21 ≈ эквивалент прежних 10×2)


## v3.1 — Выбор сценария из меню (2026-06-12)

- **Блок «Сценарий» на экране режима**: карточки из `SCENARIO_REGISTRY` (ui.js), клик переключает `localStorage` и перезагружает страницу (const-биндинги движка читают SCENARIO при загрузке)
- **Dev (index.html)**: статичный `<script src="scenarios/agency.js">` заменён лоадером — какой файл грузить, решает localStorage
- **Мульти-дист**: `node build/build.js` без флага теперь встраивает ВСЕ сценарии строками (`__SCEN_SRC`), выбранный исполняется через `new Function` (изоляция `const SCENARIO`), фолбэк — agency. `--scenario=<id>` по-прежнему собирает одиночный дист (B2B-поставки)
- **Фикс: spec-карточки стали динамическими** (`renderSpecGrid` из SCENARIO.specs) — статический HTML с агентскими специализациями ломал любой другой сценарий (`selectSpec('smm')` → краш в банке)
- **Сценарный «хром»**: интро-текст, иконка лого, title, подпись цели в хедере/графике — из SCENARIO (были захардкожены «агентство/7.5M»)

## Сценарий «Региональный банк» v0.1 (2026-06-12)

**Первый полноценный второй сценарий** — доказательство сценарной архитектуры: `scenarios/bank.js`, сборка `node build/build.js --scenario=bank` → `dist/BizTycoon-bank.html` (agency-дист не перезаписывается).

- **Тема**: сделки вместо проектов, входящий поток заявок вместо скаутинга клиентов, Q = качество портфеля, V = пропускная способность операционки. Банковские лейблы поверх engine-ролей (hasRole-логика не тронута): аналитик/операционист/управляющий/IT-инженер/продажи/комплаенс-юрист/HR
- **4 модели банка**: Розничный (+20% розница, +1 заявка), Корпоративный (+25% корп), Цифровой (−10% ФОТ, +10% скорость), Private Banking (+40% премиальные)
- **24 сделки T1–T7**: от валютных контрактов и зарплатных проектов до концессии аэропорта; банковские механики на штатных модификаторах — revenue_growth = процентный доход, payment_delay_fixed = транши казначейства, reputation = «импортёр с нюансами», nps_penalty = SLA
- **6 событий банковской специфики** с развилками: ключевая ставка ЦБ (переложить на клиентов или абсорбировать), проверка ЦБ (комплаенс-юрист спасает), слух о проблемах банка, дефолт крупного заёмщика (реструктуризация vs взыскание 50/50), кибератака (IT-инженер отбивает), приток вкладов от рухнувшего соседа
- **Дерево апгрейдов** в банковской теме (id сохранены): скоринг-модель, кредитный комитет, RPA-роботизация, «Базель по-домашнему»; **Скоринг-ИИ** на 5 уровней
- **Data-хуки staff.js**: сценарий может переопределить `roleMeta`/`roleCategories` — кандидаты генерируются с тематическими ролями без правок модуля
- **sim2-lc**: флаг `--scenario=<id>` — банк прогнан ботом: экономика жива (победа M31, профиль жёстче агентства — overhead 85K давит), 0 софтлоков

## strategy-DLC v0.2 — Monte-Carlo, события фасилитатора, отчёт (2026-06-11)

Продолжение B2B-режима, по-прежнему без правок ядра:

- **🎲 Monte-Carlo**: N прогонов (по умолч. 60) от текущего состояния до горизонта сессии; «автопилот без новых решений» ведёт контракты и добирает проекты, но не нанимает. Синхронный цикл с подменой `EventBus.emit` — без мерцания UI, реальное состояние восстанавливается. Сводка по решению §7.3: 4 ключевых показателя крупно (вероятность банкротства с вердиктом, медианный финал с квартилями, % кассовых разрывов, месяц пикового напряжения) + гистограмма распределения + детализация
- **⚡ События фасилитатора**: библиотека из 6 шоков (урезание бюджета, уход клиента, хантинг сотрудника, задержка оплаты, просадка рынка, горячий лид) + конструктор «месяц × тип × параметр». Срабатывают через обёртку `advanceMonth` — действуют и внутри Monte-Carlo (стресс-тест стратегии с заложенными шоками)
- **📄 Отчёт-артефакт**: markdown-файл одной кнопкой — вводные сессии, метрики текущей линии, все ветки с исходами, сводка Monte-Carlo, заложенные события, журнал решений
- Попутный фикс ядра (отдельный коммит): копирайтеры в пуле кандидатов + ролевой volume — V-капы проектов снова выполнимы
- Смоук-тест расширен до 23 проверок — зелёные

## strategy-DLC v0.1 — B2B-режим «Стратегическая сессия» (2026-06-11)

**Подключаемый модуль `dlc/strategy/` — ядро игры не модифицировано** (одна data-запись в реестре лоадера). Выключен DLC — игра прежняя. Первые 3 шага по `design_b2b_edition.md`:

- **Сидируемый RNG**: при старте сессии `Math.random` подменяется mulberry32-генератором; состояние в `G._rngState` → сериализуется сейвами, ветка продолжает тот же поток случайности. Одинаковый seed = одинаковая «погода» для всех участников
- **Импорт цифрового двойника** (JSON, шаблон `twin_example.json` на примере IT-аутсорс студии): баланс, overhead, штат поимённо (роль/грейд/зарплата/лояльность), действующие контракты с остатками бюджета и прогрессом — стартуют сразу в work-фазе без попапов. Win-условие отключается: сессия завершается отчётом
- **Ветки гипотез**: «⑂ Развилка» (снапшот через _snap/_restore из saves.js), «📌 Зафиксировать исход», возврат на развилку с авто-фиксацией линии; **сравнение веток** — таблица метрик (финал/реп/штат/ФОТ/сдачи) + помесячный кэшфлоу линий рядом
- UI: карточка режима на экране выбора специализации (seed, горизонт, файл двойника), панель сессии в игре, модалка сравнения — всё DOM-инжектом из модуля
- Мягкий горизонт: по достижении N месяцев — сводка ключевых показателей, игра не обрывается
- Смоук-тест `sim/test-strategy.js`: 16 проверок (двойник, детерминизм, ветки) — зелёные

## v3.0 — Единый фазовый пул: все проекты через lifecycle, тиры T1–T7 (2026-06-11)

### Один пул — один флоу
- **Все проекты «Скаутинга» идут через lifecycle-флоу** — `signProject` всегда запускает `initLCState`. Кнопка «🧪 LC-тест» и `doLifecycleScouting` удалены; бывшие тестовые проекты (Лендинг для кафе, Ребрендинг «ТехноСтарт», Онлайн-магазин «Каприз») влиты в общий пул
- **Профили цепочек** (`projects.js CHAIN_PROFILES`): instant (1 work-фаза, без ревью — разовые) / short (T1) / standard (T2–T3, 2 work) / full (T4–T5, 3 work + юридика) / epic (T6–T7, **5 work-фаз**); `phaseProfile` переопределяется в дефиниции, юридика — при requiresLegal или юристе в штате. Движок и UI генерализованы под 1–5 work-фаз

### Предоплата — переговорная механика
- Поле **`prepayChance`** у проекта (или тир-дефолт 25→65%): в шаге «Условия оплаты» появляется «Попробовать выбить аванс 30% (шанс ~X%)»; юрист +15%. Успех — аванс в кассу (−5 настроения), отказ — −3 настроения, +3 риска. У части проектов предоплаты нет вовсе — карточка скаутинга честно показывает шанс или «без предоплаты»
- Старая автопредоплата при подписании удалена; попутно закрыт баг: аванс не вычитался из бюджета (двойная выплата)

### Тиры T1–T7
- Бюджеты: T2 2–4M (фазовая себестоимость), T3 5–9M, **T5 18–35M, T6 35–70M, T7 75–150M**; нагрузки 4/8/14/18/24/32/40; длительности 4/7/10/12/14/17/20; milestone T5–T7 — 25%×3
- Гейты: T5 rep≥85+портф.30, T6 rep≥90+портф.50, T7 rep≥95+портф.80; редкость **legendary** (rep≥90); 7 новых проектов: Федеральный банк, Ритейл-сеть «Полка», Авиакомпания «Высота» (T5), «Недра», Экосистема «Сфера» (T6), Цифровизация региона, Глобальный бренд «Atlas» (T7)

### Разовые проекты
- Работают через назначение команды, как все: instant-цепочка, `_duration` 1 — при мощности ≥ нагрузки готовы за месяц; бронирование (`_scheduled`/`toggleOneTimeSchedule`) удалено; оплата через finishDelivery

### Сопутствующие фиксы и баланс
- **finishDelivery не начислял портфолио** — LC-сдачи не двигали гейты тиров; исправлено (+тир×3)
- **Дедлайн с буфером ×1.6+2 мес**: `_duration` описывает идеальный темп, реальный фазовый цикл длиннее — штраф наказывает плохое ведение, а не сам флоу (закрывает хроническую утечку репутации)
- Валидация sim2-lc (бот: команда 4, Q-перки, разовые в приоритете): победы M27–M35, медиана финала ~5.2M, банкротства редки, 0 софтлоков. Winrate бота ~15–20% — фазовая партия глубже и сложнее v2.6; калибровка под живую игру — после плейтеста
- Классический симулятор `sim/sim2.js` устарел (нефазового пути больше нет), актуален `sim/sim2-lc.js`

## v2.9 — Поэтапная оплата (п.27) + автоназначение в quick-цепочке (п.28) (2026-06-11)

### п.27 — «Поэтапная оплата (3 части)» теперь платит
- `advancePhase` (projects.js): при завершении каждой work-фазы у клиента с тегом `payment_staged` выплачивается **треть `_originalBudget`** (округление до 5K); гард `_stagedPaid < 3` — повторное прохождение фазы после правок не задваивает выплату; недоплаченный остаток приходит при сдаче
- Выбор «Поэтапной» отключает дефолтные tier-milestone (T2 40%@50) — схемы не складываются
- ⚖️ Баланс (sim2-lc, бот чередует схемы): этапные выплаты исправно капают (8–15 за ран), но схема проигрывает «Авансу 30%» по кэшфлоу (0/10 побед против 4/10) — трети приходят в КОНЦЕ фаз. Кандидат на буст: платить треть при старте каждой work-фазы

### п.28 — Quick-цепочка: автоназначение команды
- При входе quick-проекта (цепочка без `planning`) в work_0 свободные сотрудники подключаются автоматически (лог «📌 … команда подключена автоматически»); занятые на других проектах не трогаются
- Вместе с чипами v2.8 закрывает проблему «quick-проект ползёт на одном фаундере»

### sim2-lc
- Бот чередует схему оплаты по ранам (чёт — аванс, нечет — поэтапная), перехват addLog считает срабатывания механик; 0 софтлоков на серии 20

## v2.8 — Назначение команды в один клик с карточки проекта (2026-06-11)

После v2.7 назначение сотрудников — единственный рычаг распределения мощности; поднято на видное место:

- **Интерактивные чипы всей команды на карточке** (регулярные + LC work-фазы): свой чип (бирюзовый, ✕) = снять с проекта; свободный (пунктир) = назначить; занятый на другом проекте (янтарный, ↪) = перевести сюда одним кликом. У каждого чипа — имя и мощность (+N ед.), фиксированный чип «👤 Ты +2» напоминает про фаундера
- **Заголовок ряда**: «👥 Команда на проекте · ⚙ thr/load мощн. · eff%» + бар; при нехватке — янтарная подсказка «не хватает N ед. — кликни по свободным чипам»; кнопка «Подробнее» → прежний модал
- **Фикс модала назначения**: WU считался устаревшей копией формулы без нормализации qStat (занижал мощность всех сотрудников) — теперь единый `calcStaffWorkUnit`
- **Синхронизация превью**: кэп эффективности 2.5 → 1.5 в карточке и модале (как в advanceMonth с v2.4)

## v2.7 — Удалена система фокуса (2026-06-11)

Фокус-веса не имели игрового эффекта с реворка v2.x: прогресс проекта считается от назначенной команды (`getProjectThroughput`, WU-система). Удалено всё:

- **engine.js**: `setFocus`, `liveUpdateFocus`, `adjustFocusBy`, `equalFocus`, `clearFocus`, `setFocusForMonths`, поле `_focus` и перераспределение при подписании; сигнал `focus_changed`. Усталость считается от **реальной** нагрузки: `effectiveLoad = totalLoad + scheduledOneTimeLoad` (фокус-взвешивание убрано)
- **ui.js**: `_uiFocusChanged`, хедер фокус-бюджета с кнопками «↔ Равномерно»/«✕ Сбросить», слайдеры на карточках, ввод «За X мес.», блокировка «Завершить месяц» при фокусе >100%. Вместо них: **хедер мощности** («Мощность: занято X из Y ед.», перегруз — красным) и **строка темпа** на карточке («⚙️ Команда: N чел. · thr/load ед. · +X%/мес · ~M мес.») — превью по той же формуле, что advanceMonth
- **Бронирование разовых** сохранено, формулировки переведены с «% фокуса» на единицы мощности
- **game.css**: стили слайдера удалены; **events.js**: сигнал убран из справочника
- Валидация: sim2-lc 7/10 побед (0 софтлоков), классика sim2 — структура прогрессии не изменилась

## v2.6 — LC-экономика: без бесплатной команды, проходимость 7.5M (2026-06-11)

### Бесплатные исполнители убраны
- **`doLifecycleScouting`** больше не инжектирует тестовую команду (_lc_dev_1/_lc_dev_2, salary 0) — LC-проекты играются с реальным наймом; без сотрудников work-фазы идут только на мощности фаундера (2 ед.)

### Ребаланс зарплат (staff.js GRADE_CFG)
- Junior 25–45K → **20–35K** · Middle 55–100K → **40–70K** · Senior 95–160K → **80–130K** (≈ −20%: команда из 2–3 спецов окупается LC-циклом)
- Lead 190–310K → **170–280K** · Star 280–560K → **250–480K** (премиальный тир)

### Стоимость LC-проектов (fixedBudget)
- lc_simple (T1, 6 мес): **700K–1M** · lc_risky (T2, 7 мес): **2.4–3.4M** · lc_full (T2, 9 мес): **3–4.2M**
- Расчёт от burn команды (2–3 спеца ≈ 145–200K/мес) × реальный цикл с ревью; аванс 30% и engine-milestone T2 (40% при 50%) дают кэшфлоу внутри цикла

### Валидация (sim2-lc, бот: найм до 3, два параллельных проекта, помесячная расстановка)
**~55–60% побед** (M16–M34, медиана ~M29), таймауты финишируют с 3.5–6M, банкротства редки (≈1/10), уходы клиентов — 0. Игра проходима на LC-проектах, но требует менеджмента настроения и сроков. Наблюдение для тюнинга: реальный LC-цикл (12–17 мес у T2) заметно длиннее `_duration` (7–9) — штрафы просрочки бьют почти всегда (реп в таймаутах падает до 12–35)

## v2.5 — Единая система оценки клиента: mood + NPS (фикс п.26) (2026-06-11)

### Объединение двух систем (п.26)
- **`nudgeClientRating(c, delta)`** (engine.js) — единый канал изменения отношения клиента: для LC-проектов пишет в `_lcClientMood` и зеркалит в `clientNPS`; для обычных — в `clientNPS` как раньше. Все источники переведены на него: `nudgeAllNPS` (события algo/conflict/discount), удар усталости в `advanceMonth`, `investInClient`, событие «discount → Отказать» (agency.js)
- **`updateAllNPS`**: LC-клиенты исключены из старых драйверов (Q/V-гэп −14/−10, overload, manager/HR, шум) — их отношение формируется решениями фаз, work-событиями и действиями игрока; помесячно применяются только модификаторы проекта (`nps_passive`/`nps_drain`), churn-порог общий (<25)
- **`moodDelta`** (projects.js) теперь мгновенно зеркалит mood в `clientNPS` — **фикс «Промежуточного показа»**: эффект (+10 😊) был невидим, т.к. карточка проекта рендерит NPS, а тот жил отдельной жизнью
- **`initLCState`**: стартовое настроение = `npsStart` проекта (было фикс. 60) — дифференциация клиентов (lc_simple 78, lc_risky 58) работает и в LC-флоу

### Валидация (sim2-lc, 10 прогонов без --lc-shield)
Мгновенный churn исчез: lc_simple сдаётся стабильно (был 0/26 у всех LC), уходы сместились с M2–M6 на M11–M32 и стали следствием накопленного негатива (усталость → mood-удары, события, дренаж lc_risky) при нехватке денег на менеджмент настроения. Зеркало mood→NPS — 0 расхождений. Экономика LC-цикла по-прежнему отрицательная — ждёт п.27 (поэтапные выплаты)

## sim2-lc — тест lifecycle-флоу (2026-06-11)
- **`sim/sim2-lc.js`** — LC-харнесс: fake-DOM с реестром, бот «кликает» решения фаз в lc-модалках как игрок; флаг `--lc-shield` (what-if: LC вне старого NPS-канала). **`sim/debug-lc.js`** — помесячный дамп одного LC-проекта
- Серия 10×3 конфигурации: **флоу механически работоспособен** (~600 решений, 0 софтлоков), но найдены **п.26 [КРИТИЧНО]** (старый NPS-канал убивает LC-проекты до ревью — 0/26 сдано), **п.27** («Поэтапная оплата» — тег без выплат) и **п.28** (quick-цепочка без планирования — команда не назначается). Экономика LC-цикла отрицательная даже при идеальной игре — детали в `backlog/04_simulations.md` (SIM2-LC)

## v2.4 — Фикс мощности персонала (п.25) + ребаланс темпа партии (2026-06-11)

### Критический фикс: найм снова даёт мощность
- **`calcStaffWorkUnit` (engine.js)** понимает грейды staff v2 (`junior:2, middle:4, senior:7, lead:9, star:12`) и нормализует qStat-шкалу 0–10 → 0–100. Раньше любой сотрудник давал ≈1 WU независимо от грейда и зарплаты (п.25, найдено sim2: 0/150 побед)
- **`_recomputeWU` (staff.js)** делегирует в `calcStaffWorkUnit` — единая формула вместо двух расходящихся

### Ребаланс темпа (валидирован sim2, 200 партий)
- **`startReputation: 60`** — новый сценарный параметр (engine читает из settings, дефолт 100): T2 открыт со старта, T3 (rep≥70) и T4 (rep≥80) нарабатываются завершениями
- **Длительности тиров** синхронизированы с моделью затрат WIP-бюджетов: T1 3→4, T2 4→7, T3 5→10, T4 5→12 мес (явный `duration` у проекта по-прежнему приоритетен)
- **Milestone-выплаты подняты**: T2 — 40% при 50% (было 30%); T3 и теперь также T4 — 30%+30% при 33%/66% (было 25%+25% только T3) — сглаживают кассовый разрыв длинных контрактов без роста суммы
- **Кэп efficiency 2.5 → 1.5** — сильная команда ускоряет проект, но не схлопывает 9 месяцев в 3.5
- **Зарплаты топ-грейдов** (staff.js): lead 150–250K → 190–310K, star 220–450K → 280–560K — премиум-раш дороже

### Итог по симуляциям (40 партий × стратегия)
Lean 0 побед / пол ~1.8M · Balanced (2 чел.) 0 побед, почти без риска · Trio (3) — изредка побеждает к M34 · **Growth (4) — оптимум: 82% побед, в среднем M21** · Aggressive (6, premium) — быстрее (M15), но 35% банкротств. Кривая риск/награда монотонная, Lean-доминирование (ребаланс п.10) снято. Файн-тюнинг — после ручного плейтеста.

## sim2 — headless-симулятор на реальном движке (2026-06-11)
- **`sim/sim2.js`** — новый балансовый харнесс: загружает настоящие `constants → events → agency → engine → staff` в Node VM с fake-DOM; боты играют через реальные функции движка (`doScouting`, `signProject`, `hireCandidate`, `assignStaffToProject`, `advanceMonth`). Формулы не дублируются — что в игре, то и в симуляции
- 5 стратегий (lean / balanced / trio_t3 / growth / aggressive), запуск `node sim/sim2.js [N] [--fix-wu]`
- Серия 2×150 партий выявила **критический баг п.25** (calcStaffWorkUnit не знает грейдов staff v2 → любой сотрудник даёт ≈1 WU; 0/150 побед) и валидировала фикс (68/150 побед, правильная прогрессия найма) — детали в `backlog/04_simulations.md`
- Старый `sim/sim.js` (модель v0.32) признан неактуальным

## v2.1–v2.3 — Реворк проектов, персонала и экономики хода (2026-06-06 → 2026-06-09, восстановлено ретроспективно)

> Нумерация поднята v0.33 → v2.x: масштаб изменений — поколение, а не инкремент. Запись восстановлена 2026-06-11 по git-истории (`v.2.1` 53eaf6d, `v.2.2` 54d60a0, `v.2.3` 68b397c) и фактическому коду. Распределение по версиям: **v2.1** — Staff v2 + реворк игрового флоу; **v2.2** — Project Lifecycle + реворк сборки; **v2.3** — крупная итерация UI (+596 строк ui.js), доработки engine/lifecycle/сценария. Редактор сценариев v2 и DLC-архитектура добавлены чуть раньше, между v.16 и v2.1 (коммиты 31e743f, d82fd1f) вместе с деплоем на GitHub Pages.

### Project Lifecycle v2 (`src/projects.js`, новый модуль ~1700 строк)
- Проекты проходят **10 фаз F1–F10**: КП → Переговоры → Бриф → Юридика (опционально) → Планирование → Работа I–III → Ревью → Сдача; реализация по `backlog/design_project_lifecycle.md`
- **Event-фазы** (мгновенные решения) и **progress-фазы** (фоновые, через tick-механизм engine)
- Решения создают **теги и модификаторы** (`authority_pitch`, `price_pitch`, `tight_deadline` и т.д.), влияющие на поздние фазы: clientMood, risk, budget, timeline
- Три уровня переговоров через `_negotiationTier`: `quick` (2 решения), `standard` (4–5), `challenge` (5-шаговый пинг-понг + стандартный хвост); `buildPhaseChain` собирает цепочку фаз под проект

### Staff v2 — персонажи (`src/staff.js`, новый модуль ~900 строк)
- Специалисты стали **генерируемыми персонажами**: имя из пула, грейд, индивидуальные статы (quality/volume/capacity/throughput/speed), **трейты** и зарплатные ожидания
- **Скаутинг кандидатов** (`scoutCandidates`) и модал найма с карточками; **вопросы-интервью** перед наймом; торг по зарплате (`hireCandidate(id, salary)`)
- `processStaffMonth` — помесячная обработка персонала; миграция старого формата команды (`migrateStaffArr`) для обратной совместимости сейвов
- Совместимость с engine сохранена через legacy-поля (`s.role`, `s.quality`, `s.cost`, `s._iid`)

### Экономика рабочих дней
- Новый ресурс хода: **10 рабочих дней в месяц** (`actionsPerMonth`, `G.actions`); скаутинг, найм и операции стоят дни; HR-роль снижает стоимость найма до 1 дня
- **Win condition поднят до 7 500 000 ₽** (был 3M); intro-текст обновлён
- Индивидуальный `throughput` у каждого сотрудника вместо командной константы `base_throughput=10` (`getTeamThroughput` упразднён)

### Редактор сценариев v2 (`src/scenario-editor.js`, новый модуль ~1500 строк)
- Создание и менеджмент кастомных сценариев поверх `scenarios/agency.js`; хранение в `localStorage` (`bt_scenarios_v1`), модификация SCENARIO in-place до загрузки engine
- Библиотека пресетов (`scenarios/presets/index.js`): готовые сущности персонала/проектов/событий из базового сценария

### DLC-система (`dlc/loader.js`, новый модуль)
- Реестр DLC с манифестами, enable/disable через `localStorage`, динамическая подгрузка скриптов/стилей, хуки: `game_started`, `month_end`, `project_completed`, `game_ended`
- **Roguelite DLC v0.1 (stub)**: манифест и хуки зарегистрированы, механики в разработке — см. `backlog/05_roguelite.md`

### WIP после v2.3 (2026-06-10, первый ребаланс под новую экономику)
- Overhead 20K → **35K/мес** (аренда, ПО, связь)
- Бюджеты тиров пересчитаны от реальных затрат команды: T1 90–165K → **500–950K**; T2 → **1.5–3M**; T3 → **4–8M**; T4 → **10–20M**
- Стартовые oneTime (Экспресс-аудит, Разовая консультация): 70–90K → **220–350K**
- Сопутствующие правки engine.js / projects.js

### Известные долги версии (на 2026-06-11)
- `sim/sim.js` несовместим с lifecycle и рабочими днями — баланс v2.x не валидирован, достижимость 7.5M не проверена
- Ребаланс-бэклог п.10/п.6 ссылается на упразднённые константы — требует переформулировки
- UI-аудит главного экрана: `backlog/06_ui-audit.md` (2026-06-07), внедрение не начато

## v0.33 — UI: бронирование ресурса для разовых проектов (2026-06-01)
- Слайдер фокуса у разовых проектов заменён на блок бронирования: показывает «Нужно X ед. · Доступно Y из Z» и кнопку **📋 Выполнить в этом месяце**. После подтверждения — зелёный пилл «✓ Выполнить в этом месяце · −X ед.» с кнопкой «Отложить»
- При наличии запланированных разовых хедер фокуса показывает: «✦ Резерв фокуса: N% · доступно для регулярных: X из Z ед.»
- Предупреждение ⚠ на кнопке бронирования если выделение разового создаст перегруз для регулярных проектов

## v0.32 — Нагрузка разовых проектов в общей нагрузке (2026-06-01)
- **П.24 [БАГ]** Запланированный разовый (`_scheduled: true`) теперь резервирует `getProjectLoad(c)` единиц из общего throughput. `availableForRegular = throughput − scheduledOneTimeLoad` используется в `overloadPenalty` регулярных проектов. `effectiveLoad` для усталости включает `scheduledOneTimeLoad`. Добавлены `getScheduledOneTimeLoad()` и `toggleOneTimeSchedule(cid)`. Аналогичная правка в `liveUpdateFocus` и `setFocusForMonths`

## v0.31 — Полная переработка системы фокуса (2026-06-01)

### Корневой фикс: резерв больше не «утекает» в другие проекты
- `overloadPenalty = min(1, throughput / totalLoad)` от ПОЛНОЙ нагрузки заменил `lratio` в расчёте прогресса. 80% фокус всегда даёт ровно ×0.8 скорости — не больше при резерве

### Разовые проекты реагируют на фокус
- Прогресс-бар для разовых; завершение по достижении 100% прогресса, а не по истечению времени
- Фокус 100% → 1 мес.; 50% → 2 мес.; 0% → не выполняется. По умолчанию 100% (поведение без изменений)

### UX фокуса
- Фокус-бар при 1+ проекте (не только 2+); хедер — фиксированная высота, нет layout-сдвигов
- Кнопки **↔ Равномерно** и **✕ Сбросить**
- Ввод **«За X мес.»** — авто-расчёт нужного % с учётом прогресса, нагрузки, speed и усталости

## v0.30 — Фикс системы фокуса (4 бага) (2026-06-01)
- **БАГ 1** Speed-перки (Agile/Scrum/Автоматизация) не учитывались в превью `+%/мес` — добавлен `getSpeed()` в расчёт `focusBar` (ui.js) и `liveUpdateFocus` (engine.js). Ранее превью занижало прогноз на величину speed-бонуса (до +45%)
- **БАГ 2** `payment_delay_fixed`-проекты в периоде ожидания включались в `focusable` в ui.js (фильтр `!c.oneTime`), хотя `advanceMonth` их исключает. Итог: `totalFocusW` завышался → `btn-advance` блокировался ложно. Фильтр приведён в соответствие с `focusActive` в engine.js
- **БАГ 3** При перетаскивании слайдера превью (`+%/мес · ~X мес.`) обновлялось только для перетаскиваемого проекта. Остальные проекты показывали устаревшие значения несмотря на изменение `lratio`. `liveUpdateFocus` теперь считает и эмитит `previews` для всех focusable проектов; `_uiFocusChanged` обновляет все
- **БАГ 4** `totalFocusPct` в `renderGame` (ui.js) был дубликатом `totalFocusW` — одна и та же редукция. Удалён, везде используется `totalFocusW`

## v0.29 — Фикс дублирования метки месяца в логе (2026-05-31)
- **П.19 [БАГ]** Убран `${monthLabel()}:` из тела `addLog`-строки расходов в `advanceMonth` — метка месяца уже добавляется автоматически через поле `month` объекта лога

## v0.28 — Фикс capacity при уходе сотрудника (2026-05-31)
- **П.1 [БАГ]** `checkCapacityExceeded` теперь вызывается в обоих сценариях ухода: ручное увольнение (`fireStaff`) и уход из-за усталости (fatigue-quit в `advanceMonth`). После удаления сотрудника из `G.staff` показывается модал выбора — какой проект расторгнуть, если активных клиентов больше чем `getCapacity()`

## v0.27 — Фикс событий без проектов (2026-05-31)
- **П.23 [БАГ]** События «Клиент просит скидку» и «Алгоритм сменился» больше не срабатывают без активных проектов: добавлен флаг `requiresClients: true` в `scenarios/agency.js`; в `advanceMonth` фильтр исключает такие события если нет ни одного активного не-разового проекта

## v0.26 — Нейросеть (2026-05-31)
- **Новая вкладка «🤖 Нейросеть»**: покупается за 200K при репутации ≥50; три состояния — locked (экран покупки), upgrading (обучение), active (чат + дерево)
- **`src/ai.js`**: LLM-клиент с прямыми запросами к Anthropic API; API-ключ в `localStorage`; промпт включает полный снимок стейта (баланс, команда, проекты, нагрузка, усталость); fallback-советы при отсутствии ключа
- **Дерево прокачки (6 уровней)**: каждый уровень стоит деньги + N месяцев обучения; во время обучения нейросеть недоступна; визуальные узлы: ✓ завершён / ◀ текущий / 🔄 обучение / серый заблокирован
- **Система задержки ответов**: Ур.0 — 3 мес., Ур.1 — 2 мес., Ур.2 — 1 мес., Ур.3+ — мгновенно; отложенный ответ доставляется в `advanceMonth` с уведомлением и бейджем на вкладке
- **Лимиты запросов**: 2/мес на Ур.0–1, 3/мес на Ур.2, 5/мес на Ур.3, ∞ на Ур.4–5
- **Пассивные бонусы от ИИ**: Ур.2 +2Q/мес, Ур.3 +2Q +1реп/мес, Ур.4–5 +3Q +2реп +5V/мес; учитываются в `getQuality()` и `getVolume()`; Ур.5 — авто-скаутинг
- **Быстрые вопросы**: 7 шаблонов для одного клика; чат с bubble-UI; поле API-ключа (скрыто когда ключ задан)
- **EventBus**: 5 новых сигналов (ai_purchased, ai_upgrading, ai_training_complete, ai_thinking, ai_response_ready)

## v0.25 — Редизайн панели метрик (2026-05-30)
- **Метрики — grid-карточки**: каждая метрика — отдельная карточка `var(--bg2)` с uppercase-меткой; сетка 2 колонки, wide-метрики (Q, V, Портфолио, Нагрузка, Усталость) занимают всю ширину
- **Кнопка `?`** в правом углу каждой карточки: по клику раскрывает подсказку с объяснением метрики; повторный клик или клик на другую `?` закрывает; только одна подсказка открыта одновременно
- **Состав метрик без изменений**: Q (с баром порогов), V (с баром), Портфолио (бар + мультипликатор), Слоты, NPS, Репутация, Скорость, Нагрузка/Произв., Усталость · прогноз, Overhead
- Вспомогательная функция `mc({id, label, value, ...})` генерирует карточку; `toggleMetricTip(id)` управляет видимостью подсказок

## v0.24 — Пересборка UI: разгрузка правой колонки (2026-05-30)
- **П.2 [ДОРАБОТКА]** Реструктурирован игровой layout: правая колонка с 5 панелей → 3; левая дополнена аналитикой
- **Метрики переехали в левую колонку** — Q/V/нагрузка/усталость/портфолио теперь рядом с активными проектами и P&L; логически правильное место для принятия решений
- **Команда + Найм объединены** в одну панель: текущий состав сверху, секция «Нанять» раскрывается по клику; стрелка `▸/▾` показывает состояние; закрыта по умолчанию
- **Апгрейды — аккордеон по 3 категориям**: 🎯 Качество (4 перка) / ⚡ Скорость (3 перка) / 😴 Восстановление (3 перка); первые две открыты по умолчанию, Восстановление закрыто; бейдж `✓ N` показывает количество купленных в группе
- **`_acc` объект** в ui.js хранит состояние аккордеонов между ре-рендерами; `toggleAcc(key)` обновляет только DOM без полного перерисовывания
- Кнопка «Завершить месяц» перемещена ниже метрик (после аналитического контекста)

## v0.23 — EventBus / Godot-совместимая архитектура (2026-05-29)
- **`src/events.js`** — новый модуль EventBus: `on/off/emit`; аналог сигналов Godot; загружается первым; содержит справочник всех 10 сигналов с Godot-эквивалентами
- **engine.js DOM-free**: все прямые вызовы DOM убраны из engine; `notify/goTo` → `EventBus.emit`; `renderGame/showEvent/endGame` → `_emitRender/_emitShowEvent/_emitEndGame`; `showScoutResults/closeScout/showConfirm` → emit; `liveUpdateFocus` вычисляет данные и emit-ит `focus_changed` вместо прямого DOM-обновления
- **ui.js — подписчик**: `initEventBus()` биндит все 10 сигналов на реальные DOM-функции (`_uiNotify`, `_uiNavigate`, `_uiSelectSpec`, `_uiShowScout`, `_uiCloseScout`, `_uiShowConfirm`, `_uiFocusChanged`); `resetGame` вызывает `initEventBus()` после `initState()`
- **saves.js** обновлён: `renderGame()/goTo()` → `EventBus.emit('render'/'navigate')`
- **`_legacyShowScout`** — tombstone-функция в engine.js, временно используется ui.js для рендера карточек скаутинга; помечена к полному переносу при миграции в Godot
- **Порядок сборки**: constants → events → scenario → engine → ui → saves

## v0.22 — Система сохранений (2026-05-29)
- **`src/saves.js`** — новый модуль: авто-сохранения после каждого месяца (кольцевой буфер 48 слотов) + ручные сохранения (10 слотов); полный откат на любой шаг через `loadSave(id)`
- **Ctrl+S / Cmd+S** — быстрое ручное сохранение из любого места игры; Escape закрывает модал
- **Кнопка 💾** в правом углу игрового хедера — открывает модал со списком всех сохранений
- **Модал сохранений**: две секции (📌 Ручные / 🔄 Авто), время и баланс каждого слота, кнопки «Загрузить» и удаления ручных сохранений
- **Авто-сохранение** вызывается в `advanceMonth` до win/lose-проверки — откат работает даже из финального состояния
- Хранение в `localStorage` (`btz_saves_auto`, `btz_saves_manual`); G сериализуется полностью (лог обрезается до 80 записей)

## v0.21 — Сценарная архитектура (2026-05-29)
- **Архитектура**: бизнес-данные вынесены из `src/data.js` в отдельный слой сценариев
- **`scenarios/agency.js`** — единый файл текущего сценария «Диджитал-агентство»; содержит `SCENARIO` объект с полями: `settings`, `specs`, `staff`, `staffRoles`, `roleLabels`, `budgetRanges`, `projects`, `upgrades`, `events`
- **`src/constants.js`** — сценарно-независимые данные: `MONTHS`, `CASE_GRADES`
- **`src/data.js`** — упразднён, оставлен как tombstone с комментарием
- **`src/engine.js`** и **`src/ui.js`** — получают данные через алиасы (`const STAFF_DEFS = SCENARIO.staff` и т.д.); внутренний код не изменился
- **`build/build.js`** — поддерживает флаг `--scenario=<id>`; порядок сборки: constants → scenario → engine → ui
- **`index.html`** — обновлён порядок `<script>` тегов под новую архитектуру
- **Добавить новый сценарий**: скопировать `scenarios/agency.js` → `scenarios/bank.js`, заменить данные, запустить `node build/build.js --scenario=bank`

## v0.20 — Фикс расчёта фокуса (2026-05-29)
- **П.22 [БАГ]** Исправлен некорректный расчёт `focusMult` в `advanceMonth`: ранее нормировался через `totalFocusW` (сумму выставленных фокусов), из-за чего 20%+0%+0% давало проекту ×3.0 вместо ×0.20 — нагрузка не снижалась, усталость росла
- `focusMult = _cFocus / 100` — теперь нормируется через 100; `effectiveLoad = totalLoad × (totalFocusW/100)` — усталость считается по реальной доле работы; `loadRatio = throughput / effectiveLoad`
- Исправлено в `ui.js`: превью `+%/мес` в конфигураторе фокуса и прогноз усталости под шкалой — оба зеркалят новую формулу

## v0.19 — Growth-стратегия: стартовый капитал и milestone-выплаты (2026-05-29)
- **Ребаланс A2**: стартовый капитал поднят 750K → **1 000 000₽** — runway при нулевом доходе теперь ~50 мес. vs 37 ранее
- **Ребаланс A3 — Milestone-выплаты T2/T3**: T2-проекты выплачивают **30% бюджета при 50% прогресса**; T3-проекты — **25% при 33%** и **25% при 66%**; итоговая выплата при сдаче уменьшается на выплаченное
- **UI milestone**: маркеры на прогресс-баре (оранжевый до выплаты → зелёный после); строка «💵 Получено milestone: +Xk» / «⏳ Milestone при N%: +Xk»; зачёркнутый полный бюджет рядом со строкой «при сдаче» после первого milestone
- **Симуляционная проверка**: Growth 0/3 выжил (v0.18) → **2/3 выжил (v0.19)**; единственный банкрот на M28 (vs M4–M9 ранее); T3-milestones давали 240–320K на M3/M5, полностью покрывая ранний burn

## v0.18 — Реворк механики усталости (2026-05-27)
- **Реворк п.21 — Активные инструменты восстановления**: 3 новых действия в апгрейдах: Оплачиваемые выходные (12K, −12 уст., кулдаун 1 мес.), Тимбилдинг (28K, −22 уст., кулдаун 2 мес.), Корпоративный отпуск (55K, −38 уст., кулдаун 3 мес., доступен при уст. ≥40)
- **Реворк п.21 — Пассивное восстановление**: порог восстановления поднят с <60% до <70% загрузки; скорость откатa увеличена с −5 до −8/мес; зона нейтральной нагрузки (70–85%) даёт +1/мес вместо прежнего перехода
- **Реворк п.21 — HR-грейды**: HR Jr −20% к росту, HR Md −30%, HR Sr −45% + пассивный −2/мес всегда (ранее любой HR давал плоский −30%)
- **Реворк п.21 — Прогноз усталости**: под шкалой усталости в метриках — «+X/мес при текущей нагрузке · Норма через ~N мес.» или «рекомендуется Тимбилдинг»
- **Реворк п.21 — Кулдаун-система**: `G.fatigueActionCooldowns` — счётчики декрементируются в `advanceMonth`; кнопки апгрейдов показывают «через N мес.»; бейдж «😴 Усталость −X» вместо «Q +0»

## v0.17 — Грейды специалистов, расширение пула, Speed (2026-05-27)
- **Фич п.1 — Грейды специалистов**: 21 специалист (3 грейда × 7 ролей): Junior (дешевле, слабее), Middle (базовый), Senior (дорогой, сильный; открывается при rep≥70/80). Интерфейс найма — коллапсируемые секции по ролям; каждый грейд показывает зарплату, бонусы, условие разблокировки. Обратная совместимость: `hasRole`/`countRole` проверяют `s.role||s.id`
- **Фич п.2 — Расширение пула проектов**: +30 проектов суммарно; новый Tier 4 (эндгейм, rep≥80, бюджет 1.5M–3.5M, 6 проектов). Система редкости `rarity` (common/uncommon/rare/epic): badge на карточке скаутинга, цветная рамка у rare/epic. Rare открывается при rep≥60, epic при rep≥80. `_generateOffers` учитывает rarity и maxTier по репутации
- **Фич п.5 — Параметр «Скорость»**: `getSpeed()` = 1.0 + staff speedBonuses + G.speedUpgrades; `speedMult` применяется в `advanceMonth` рядом с `fatigueMult`. 3 новых перка в апгрейдах: Agile-внедрение (+10%), Scrum-мастер (+15%), Автоматизация процессов (+20%). Пассивный бонус от Manager Middle/Senior (+5%/+8%) и Developer Senior (+8%). Метрика «Скорость» в правой панели с цветовой шкалой и подсказкой по источникам бонуса

## v0.16 — Кулдаун и фикс-бюджет для oneTime-проектов (2026-05-27)
- **Ребаланс п.9** oneTime-проекты с `cooldown` (Экспресс-аудит, Разовая консультация) теперь доступны раз в 3 месяца — нельзя строить агентство только на разовых заказах
- `fixedBudget: [70 000–90 000₽]` — выплата случайна в диапазоне: иногда небольшой плюс, иногда небольшой минус относительно overhead; не позволяет строить агентство только на разовых заказах
- В скаутинге на заблокированной карточке отображается «⏳ Следующий разовый заказ через N мес.»
- Счётчик `G.oneTimeCooldown` декрементируется каждый месяц автоматически

## v0.15 — Фикс отображения бюджета oneTime-проектов (2026-05-27)
- **П.20 [БАГ]** oneTime-проекты в скаутинге больше не показывают «0/мес» — теперь отображается диапазон разовой выплаты из `BUDGET_RANGES` тира (например, «90K–165K разово»)

## v0.14 — Ребаланс экономики раннего старта (2026-05-25)
- **Ребаланс п.2 — Зарплаты снижены ~28–30%**: Дизайнер 67K→48K, Копирайтер 50K→35K, Менеджер 75K→52K, Разработчик 85K→60K, SMM 48K→34K, Юрист 58K→42K, HR 42K→30K. Цель — сделать легальный старт с 2–3 специалистами выживаемым без серой зоны
- **Ребаланс п.3 — Бюджеты проектов +10%**: T1: 80–150K → 90–165K, T2: 200–400K → 220–440K, T3: 500K–1.2M → 550K–1.32M. Повышает ценность каждого завершённого контракта
- **Новые стартовые oneTime-проекты без Q/V-требований**: «Экспресс-аудит» (🔍, T1, prob 0.80) и «Разовая консультация» (💬, T1, prob 0.75). Появляются в скаутинге с первого месяца, дают кэш до найма специалистов
- **Симуляционная проверка**: до ребаланса 15/15 банкротств (avg 4.7 мес.); после — 7/8 банкротств (avg 10.4 мес.), 1 таймаут (M48)

## v0.13 — Экономика расторжения, усталость и портфолио (2026-05-24)
- **П.15 [БАГ]** Расторжение проекта с авансом теперь вычитает его обратно из баланса; юрист снижает возврат на 50%; в диалоге подтверждения показывается сумма к возврату
- **П.16 [БАГ → rework]** Усталость отображается как конкретное число: в метриках `Производительность: 9 (Усталость: −1)`, в превью фокуса `+14%/мес (Усталость: −2%)` — вместо абстрактных множителей `×85%`
- **П.17 [БАГ КРИТИЧНО]** Формула усталости исправлена: использовался инвертированный `loadRatio = throughput/totalLoad`, из-за чего усталость росла при отсутствии проектов и замедлялась при перегрузе; теперь — `loadPct = totalLoad/throughput`
- **П.18 [БАГ]** Расторгнутые (`terminated`) и провальные (`failed`) проекты исключены из списка доступных для кейсов в портфолио (бейдж вкладки и рендер `renderPortfolioTab`)

## v0.12 — Исправление критических багов (2026-05-24)
- **П.8 [БАГ]** Кнопка «Завершить месяц» теперь `disabled` при суммарном фокусе >100%; снимается автоматически при ≤100%
- **П.9 [БАГ]** Portfolio-мультипликатор теперь виден на карточке проекта: показывается `+N% портфолио` под суммой бюджета (применялся в `completeProject` корректно, но был невидим)
- **П.10 [БАГ]** Превью фокуса (`+%/мес`, `мес. до завершения`) учитывает `fatigueMult` — совпадает с реальным расчётом; добавлен хелпер `getFatigueMult()` в engine.js
- **П.11 [БАГ]** Предоплата реально зачисляется в баланс: `signProject` искал `_prepaymentPossible` в `PROJECT_POOL` (где флага нет) → теперь берёт из `G.scoutPool`
- **П.12 [БАГ]** Проект с прогрессом 99.99+% больше не блокируется: `completeProject` использует `Math.round` для сравнения вместо сырого float
- **П.13 [БАГ]** Прогресс округляется до 2 знаков при каждом шаге (`Math.round(...*100)/100`) — устраняет систематический недобор; превью фокуса показывает целые числа; инпут фокуса получил `step="1"`

## v0.11 — Усталость команды (2026-05-24)
- **Фич п.11 — Усталость команды**: новый параметр `G.teamFatigue` (0–100) в метриках; растёт при загрузке ≥85% (+4–10/мес), восстанавливается при <60% (−5/мес); HR снижает рост на 30%
- **Пороговые эффекты**: Напряжение (30+) — ×95% прогресс, −3 NPS; Выгорание (60+) — ×85%, −5 NPS, 10% шанс ухода сотрудника/мес; Кризис (85+) — ×70%, −8 NPS, 20% уход, найм заблокирован
- **Индикатор усталости** в правой панели метрик: цветная шкала + метка состояния (Норма / Напряжение / Выгорание / Кризис)
- **Новый баг в бэклоге (п.8)**: конфигуратор фокуса не блокирует кнопку «Завершить месяц» при суммарном фокусе >100%

## v0.10 — Конфигуратор фокуса + предоплата (2026-05-24)
- **Фич п.7 — Конфигуратор фокуса**: при 2+ активных проектах на каждой карточке появляются контролы «Фокус» (−/+) и мини-бар; фокус влияет на скорость прогресса через `focusMult` в `advanceMonth`; равный фокус = нейтральный ×1.0 для всех
- **Фич п.9 — Предоплата**: 25% проектов в скаутинге помечаются «💰 Предоплата возможна»; при подписании 50% (+ 15% с юристом) шанс получить аванс 25–35% от бюджета немедленно; итоговая выплата при сдаче уменьшается на аванс; на карточке активного проекта отображается полученная сумма

## v0.9 — Слоты без менеджера, кредитные линии, нагрузка в скаутинге (2026-05-23)
- **Ребаланс п.4**: базовый слот без менеджера 1 → **2** (`getCapacity` base = 2); менеджер по-прежнему даёт +2 сверх базы
- **Кредитные линии**: кнопка «Взять кредит» в P&L при репутации ≥ 30; три тира: Базовый 50k (10k/мес × 6), Стандарт 150k (25k/мес × 7), Премиум 300k (42k/мес × 8); при невозможности оплаты − 5 репутации
- **Нагрузка в скаутинге**: чип `⚙️ N` на каждой карточке проекта; показывает текущую нагрузку команды после подписания и предупреждение о перегрузе
- **Кнопка «Нанять»**: `align-self:center; flex-shrink:0` — кнопка больше не съезжает при многострочном описании специалиста

## v0.8 — Модель фиксированного бюджета + прогресс команды (2026-05-23)
- **Реворк модели оплаты**: помесячная выручка убрана; каждый проект получает `_totalBudget` при подписании (T1: 80–150k, T2: 200–400k, T3: 500k–1.2M); оплата поступает при завершении
- **Штраф за просрочку**: −10% от бюджета за каждый месяц сверх дедлайна, максимум −40%
- **Шкала прогресса**: 0–100% на каждой карточке клиента; кнопка «Завершить» доступна только при 100%
- **Нагрузка команды**: `teamThroughput` (10 + сумма throughput сотрудников) vs `totalLoad` (нагрузка активных проектов: T1=7, T2=14, T3=24); при перегрузе прогресс замедляется пропорционально
- **Индикатор нагрузки** в секции метрик: шкала и предупреждение при overload
- **Поле throughput** у каждого специалиста (Designer=7, Developer=7, Manager=5, Copywriter=5, SMM=3, Lawyer=2, HR=2)
- **Событие «скидка»** переработано: снижает `_totalBudget` на 15% вместо помесячного дохода
- Стартовый баланс поднят 500k → **750k** (компенсация отсутствия помесячной выручки)
- P&L секция переработана: показывает пайплайн проектов + расход/мес
- `payment_delay_fixed`: период ожидания не нагружает команду (load=0)

## v0.5 — Модульная структура (2026-05-22)
- Монолитный HTML разбит на `src/data.js`, `src/engine.js`, `src/ui.js`, `styles/game.css`
- Добавлен `index.html` — dev-точка входа
- Добавлен `build/build.js` — сборка в единый portable HTML (`dist/BizTycoon.html`)
- Добавлен `.gitignore`

## v0.4 — Портфолио, специалисты, Q-апгрейды (2026-05-22)
- Параметр Портфолио: накопление по тирам, мультипликатор выручки до +20%, 3 новых проекта (minPortfolio 12/28/50)
- 4 новых специалиста: Разработчик, SMM-маркетолог, Юрист, HR-менеджер
- Возможность нанимать несколько специалистов одной роли
- 4 апгрейда Q: инструменты, курсы, UX-консультант, фриланс-дизайнер
- 2 tech-проекта (требуют Разработчика): SaaS-интеграция, FinTech-платформа
- Увольнение сотрудников (выходное пособие 50%) и досрочное расторжение контрактов (−10 реп.)

## v0.3 — Q/V визуализация (2026-05-22)
- Прогресс-бары Q и V с маркерами порогов (Стартап / Корп. / Гос.)
- Подсказки «нанять Дизайнера/Копирайтера» при нулевых метриках
- Чипы ✓/✗ требований на карточках проектов в скаутинге

## v0.2 — Скаутинг и рабочие дни (2026-05-22)
- Ресурс рабочих дней (10/мес), скаутинг −3 дня, найм −2 дня
- Модальное окно результатов скаутинга
- Репутация как гейт к тирам проектов

## v0.1 — Прототип (2026-05-22)
- Базовая игра: специализация, клиенты, P&L, NPS, найм команды
- 9 типов проектов (3 тира), 4 случайных события
- Экран итогов с кривой баланса и инсайтами

